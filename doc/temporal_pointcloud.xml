<?xml version="1.0" encoding="UTF-8"?>
<!--
   ****************************************************************************
    MobilityDB Manual
    Copyright(c) MobilityDB Contributors

    This documentation is licensed under a Creative Commons Attribution-Share
    Alike 3.0 License: https://creativecommons.org/licenses/by-sa/3.0/
   ****************************************************************************
-->
<chapter xml:id="tpointcloud">
	<title>Temporal Point Clouds</title>

	<para>The <ulink url="https://pgpointcloud.github.io/pointcloud/">pgPointCloud</ulink> extension stores LIDAR point cloud data in PostgreSQL using two static types: <varname>pcpoint</varname>, a single multi-dimensional point whose dimensions are described by a <emphasis>schema</emphasis> stored in the <varname>pointcloud_formats</varname> catalog table; and <varname>pcpatch</varname>, a compressed batch of <varname>pcpoint</varname> values that share the same <varname>pcid</varname> (schema id). Each schema fixes the dimensions present (<varname>X</varname>, <varname>Y</varname>, <varname>Z</varname>, <varname>Intensity</varname>, …), their numeric type, scale, and offset.</para>

	<para>MobilityDB lifts these types into the temporal world via <varname>tpcpoint</varname> (a moving LIDAR/GPS sensor) and <varname>tpcpatch</varname> (a time series of compressed point clusters). It also adds set types <varname>pcpointset</varname> / <varname>pcpatchset</varname> and the spatiotemporal bounding-box type <varname>tpcbox</varname>. A complete reference for the static <varname>pcpoint</varname> and <varname>pcpatch</varname> types is given in the <ulink url="https://pgpointcloud.github.io/pointcloud/">pgPointCloud documentation</ulink>; this chapter covers MobilityDB's additions on top of pgPointCloud.</para>

	<para>To use the types described here, MobilityDB must be built with the <varname>POINTCLOUD=ON</varname> CMake option. On such a build the generated <filename>mobilitydb.control</filename> declares <varname>requires = 'postgis, pointcloud'</varname>, so a single <varname>CASCADE</varname> creates the full stack:</para>
	<programlisting language="sql" xml:space="preserve" format="linespecific">
CREATE EXTENSION mobilitydb CASCADE;
-- NOTICE:  installing required extension "postgis"
-- NOTICE:  installing required extension "pointcloud"
</programlisting>
	<para>Creating the extensions by hand in the wrong order, without <varname>CASCADE</varname>, raises a missing-type error.</para>

	<para>Both <varname>tpcpoint</varname> and <varname>tpcpatch</varname> support the standard MobilityDB binary I/O through <varname>asBinary</varname> / <varname>tpcpointFromBinary</varname> / <varname>tpcpatchFromBinary</varname> and through PostgreSQL's <varname>COPY ... (FORMAT BINARY)</varname>. The encoding carries the <varname>pcid</varname> of each value beside the dimension payload and, whenever the encoding backend knows the schema XML for that <varname>pcid</varname>, the schema itself, so a value dumped from one cluster imports into another that has never seen the <varname>pcid</varname>.</para>

	<sect1 xml:id="tpointcloud_schemas">
		<title>Point Cloud Schemas</title>

		<para>Every <varname>pcpoint</varname> and <varname>pcpatch</varname> value carries a <varname>pcid</varname> that resolves to a <emphasis>schema</emphasis>, which states the dimensions the value holds (X, Y, Z, …), the numeric type each is stored as, and its scale and offset. The schema is stated in SQL, in two tables: <varname>pointcloud_schemas</varname> holds what a schema states as a whole, and <varname>pointcloud_dimensions</varname> one row per dimension. A minimal 3D schema, every dimension stored as <varname>int32_t</varname> at unit scale:</para>
		<programlisting language="sql" xml:space="preserve" format="linespecific">
INSERT INTO pointcloud_schemas (pcid, srid, compression, description)
VALUES (1, 4326, 'none', 'Minimal 3D schema');
INSERT INTO pointcloud_dimensions (pcid, dim_no, dim_name, interpretation, dim_scale,
  dim_offset, active, description)
VALUES (1, 1, 'X', 'int32_t', 1, 0, true, 'Easting'),
  (1, 2, 'Y', 'int32_t', 1, 0, true, 'Northing'),
  (1, 3, 'Z', 'int32_t', 1, 0, true, 'Elevation');
</programlisting>
		<para>The size of each dimension and its offset within a point are absent because they follow from the interpretation, and the X, Y, Z and M dimensions are absent because they resolve from the names. The <varname>dim_no</varname> column states the order the dimensions are stored in, so it does not depend on the order the rows are inserted in, and <varname>dim_name</varname> is the name they resolve by. No column name needs quoting in any host these tables are materialised in. A schema is global to the database and usually registered once, at fixture-load time; the SRID is per schema and is inherited by every value carrying that <varname>pcid</varname>.</para>
		<para>A database whose schemas are already registered in the <varname>pointcloud_formats</varname> catalog of pgPointCloud, as an XML document, keeps working: that catalog is read for a <varname>pcid</varname> that the two tables above do not state.</para>
		<para>Mixing schemas is rejected at construction time: a <varname>tpcpoint</varname> sequence whose instants carry different <varname>pcid</varname> values cannot be built, and the same holds for a <varname>pcpointset</varname> and a <varname>pcpatchset</varname>. The schema is what gives the dimensions their meaning, so a value mixing schemas would carry coordinates nothing can interpret.</para>
		<para>Three helpers answer what a <varname>pcid</varname> names without a value of that schema in hand and without depending on the table layout. Each answers <varname>NULL</varname> for a <varname>pcid</varname> no schema states.</para>
		<itemizedlist>
			<listitem xml:id="pointcloud_pointCloudSchemaSRID">
				<indexterm significance="normal"><primary><varname>pointCloudSchemaSRID</varname></primary></indexterm>
				<para>Return the spatial reference system every value of a schema is expressed in</para>
				<programlisting role="syntax" xml:space="preserve" format="linespecific">
pointCloudSchemaSRID(integer) &#x2192; integer
</programlisting>
				<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT pointCloudSchemaSRID(1);
-- 4326
</programlisting>
			</listitem>
			<listitem xml:id="pointcloud_pointCloudSchemaCompression">
				<indexterm significance="normal"><primary><varname>pointCloudSchemaCompression</varname></primary></indexterm>
				<para>Return how a patch of a schema is stored</para>
				<programlisting role="syntax" xml:space="preserve" format="linespecific">
pointCloudSchemaCompression(integer) &#x2192; text
</programlisting>
				<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT pointCloudSchemaCompression(1);
-- none
</programlisting>
			</listitem>
			<listitem xml:id="pointcloud_pointCloudSchemaNDims">
				<indexterm significance="normal"><primary><varname>pointCloudSchemaNDims</varname></primary></indexterm>
				<para>Return how many dimensions of a schema hold values</para>
				<programlisting role="syntax" xml:space="preserve" format="linespecific">
pointCloudSchemaNDims(integer) &#x2192; integer
</programlisting>
				<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT pointCloudSchemaNDims(1);
-- 3
</programlisting>
			</listitem>
		</itemizedlist>
	</sect1>

	<sect1 xml:id="pcpoint_pcpatch_static">
		<title>Static Point Clouds</title>

		<para>The static types come from pgPointCloud. MobilityDB exposes the schema-aware coordinate accessors needed to project pgPointCloud values into the rest of the temporal-types ecosystem.</para>

		<sect2 xml:id="pcpoint_pcpatch_constructors">
			<title>Constructors</title>

			<para>pgPointCloud's <varname>pcpoint_in</varname> only accepts hex-WKB, so the canonical builder is <varname>PC_MakePoint(pcid, ARRAY[…]::float[])</varname>. MobilityDB ships two thin SQL overloads on top of it for shorter test literals and ad-hoc queries; the underlying schema-dimension validation still happens inside <varname>PC_MakePoint</varname>.</para>

			<itemizedlist>
				<listitem xml:id="pcpoint_pcpoint">
					<indexterm significance="normal"><primary><varname>pcpoint</varname></primary></indexterm>
					<para>Build a 2D or 3D pcpoint directly from x/y(/z) coordinates</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
pcpoint(pcid integer,x float,y float) → pcpoint
pcpoint(pcid integer,x float,y float,z float) → pcpoint
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT pcpoint(1, 10.0, 20.0, 30.0);
-- Equivalent to pcpoint(1, 10.0, 20.0, 30.0)
</programlisting>
				</listitem>

				<listitem xml:id="pcpoint_pcpoint_integer">
					<indexterm significance="normal"><primary><varname>pcpoint</varname></primary></indexterm>
					<para>Build a pcpoint from the coordinates of every dimension of the schema</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
pcpoint(pcid integer,coordinates float[]) → pcpoint
</programlisting>
					<para>The layout of the schema states every dimension, inactive ones included, which is how a schema of more dimensions than the overloads above reach is given its values. That count is the one <varname>PC_MakePoint</varname> takes and is NOT the count of active dimensions that <varname>pointCloudSchemaNDims</varname> reports</para>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT pcpoint(1, ARRAY[10.0, 20.0, 30.0]::float[]);
</programlisting>
				</listitem>

				<listitem xml:id="pcpatch_pcpatch">
					<indexterm significance="normal"><primary><varname>pcpatch</varname></primary></indexterm>
					<para>Build a pcpatch from the coordinates of its points, one point after another</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
pcpatch(pcid integer,coordinates float[]) → pcpatch
</programlisting>
					<para>Every dimension of the layout of the schema is given for each point, inactive ones included. That count is the one <varname>PC_MakePatch</varname> takes and is NOT the count of active dimensions that <varname>pointCloudSchemaNDims</varname> reports; a count that divides evenly by the wrong one of the two builds a patch of the wrong points rather than reporting an error. A patch built this way states the schema itself, there being no point to read it from</para>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT pcpatch(1, ARRAY[1.0, 1.0, 1.0, 2.0, 2.0, 2.0]::float[]);
</programlisting>
				</listitem>

				<listitem xml:id="pcpatch_pcpatch_pcpoint">
					<indexterm significance="normal"><primary><varname>pcpatch</varname></primary></indexterm>
					<para>Build a pcpatch from a variadic list of pcpoints sharing the same pcid</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
pcpatch(VARIADIC points pcpoint[]) → pcpatch
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT pcpatch(pcpoint(1, 1.0, 1.0, 1.0), pcpoint(1, 2.0, 2.0, 2.0));
-- Equivalent to pcpatch(pcpoint(1, 1.0, 1.0, 1.0), pcpoint(1, 2.0, 2.0, 2.0))
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="pcpoint_accessors">
			<title>Accessors</title>

			<itemizedlist>
				<listitem xml:id="pcpoint_pcid">
					<indexterm significance="normal"><primary><varname>pcid</varname></primary></indexterm>
					<para>Return the schema id of a pcpoint or pcpatch</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
pcid({pcpoint,pcpatch}) → integer
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT pcid(pcpoint(1, 10.0, 20.0, 30.0));
-- 1
</programlisting>
				</listitem>

				<listitem xml:id="pcpoint_getX">
					<indexterm significance="normal"><primary><varname>getX</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>getY</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>getZ</varname></primary></indexterm>
					<para>Return a coordinate of a pcpoint, NULL if the dimension is absent from the schema</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
getX(pcpoint) → float
getY(pcpoint) → float
getZ(pcpoint) → float
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
WITH pt AS (SELECT pcpoint(1, 10.0, 20.0, 30.0) p)
  SELECT getX(p), getY(p), getZ(p) FROM pt;
-- 10 | 20 | 30
</programlisting>
				</listitem>

				<listitem xml:id="pcpoint_getDim">
					<indexterm significance="normal"><primary><varname>getDim</varname></primary></indexterm>
					<para>Return any named dimension of a pcpoint, NULL if the name is unknown for the schema</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
getDim(pcpoint,text) → float
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT getDim(pcpoint(1, 10.0, 20.0, 30.0), 'X');
-- 10
</programlisting>
				</listitem>

				<listitem xml:id="pcpoint_SRID">
					<indexterm significance="normal"><primary><varname>SRID</varname></primary></indexterm>
					<para>Return the spatial reference identifier (inherited from the schema)</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
SRID({pcpoint,pcpatch}) → integer
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT SRID(pcpoint(1, 10.0, 20.0, 30.0));
-- 0
SELECT SRID(pcpatch(pcpoint(1, 10.0, 20.0, 30.0), pcpoint(1, 11.0, 21.0, 31.0)));
-- 0
</programlisting>
				</listitem>

				<listitem xml:id="pcpatch_geometry">
					<indexterm significance="normal"><primary><varname>geometry</varname></primary></indexterm>
					<para>Return the multipoint of the positions the points of a patch occupy</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
geometry(pcpatch) &#8594; geometry
</programlisting>
					<para>The schema the <varname>pcid</varname> names decides the SRID and whether the result carries a Z dimension</para>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT ST_AsText(geometry(pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2))));
-- MULTIPOINT Z ((1 1 1),(2 2 2))
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>
	</sect1>

	<sect1 xml:id="pcpointset_pcpatchset">
		<title>Point Cloud Sets</title>

		<para>A <varname>pcpointset</varname> is an ordered set of distinct <varname>pcpoint</varname> values; a <varname>pcpatchset</varname> is the analogous set type for <varname>pcpatch</varname>. Both follow MobilityDB's general set semantics (see <xref linkend="set_span_types" />): values are deduplicated on construction, the binary representation is canonical, and all generic set-level operators (<varname>=</varname>, <varname>&lt;&gt;</varname>, <varname>&lt;</varname>, <varname>&lt;=</varname>, <varname>&gt;</varname>, <varname>&gt;=</varname>, <varname>@&gt;</varname>, <varname>&lt;@</varname>, <varname>&amp;&amp;</varname>, <varname>+</varname>, <varname>-</varname>, <varname>*</varname>) and accessors (<varname>numValues</varname>, <varname>memSize</varname>, <varname>asText</varname>, <varname>asBinary</varname>, <varname>asEWKB</varname>, <varname>asHexWKB</varname>, <varname>asHexEWKB</varname>, …) are available (see <link linkend="setspan_asEWKB">the binary representations of a spatial set</link>). The <varname>pcid</varname> schema, and its SRID, is resolved out-of-band from the <varname>pointcloud_formats</varname> catalog rather than embedded in the value, so <varname>asBinary</varname>/<varname>asEWKB</varname> and <varname>asHexWKB</varname>/<varname>asHexEWKB</varname> return the same bytes for both set types.</para>

		<para>All values in a set must share the same <varname>pcid</varname>. Mixing schemas in a single set raises an error at construction time.</para>

		<sect2 xml:id="pcpointset_constructor">
			<title>Constructors</title>
			<itemizedlist>
				<listitem xml:id="pcpointset_set">
					<indexterm significance="normal"><primary><varname>set</varname></primary></indexterm>
					<para>Construct a set from an array of values, all of which must share the same pcid</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
set(pcpoint[]) → pcpointset
set(pcpatch[]) → pcpatchset
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT numValues(set(ARRAY[pcpoint(1, 10.0, 20.0, 30.0),
  pcpoint(1, 10.0, 20.0, 30.0),  -- duplicate, deduped
  pcpoint(1, 11.0, 21.0, 31.0)]));
-- 2
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>
	</sect1>

	<sect1 xml:id="tpcbox">
		<title>Point Cloud Bounding Boxes</title>

		<para>The <varname>tpcbox</varname> type is the spatiotemporal bounding box for the temporal point-cloud types. It mirrors the structure of <varname>stbox</varname> (see <xref linkend="box_types" />), same X/Y/Z/T axes, same flag bits for which dimensions are present, and adds a single field: the <varname>pcid</varname>. Two <varname>tpcbox</varname> values with different <varname>pcid</varname> values describe different schemas, and an operation between them raises an error instead of comparing their geometric or temporal extent.</para>

		<para>Carrying the <varname>pcid</varname> through bounding-box arithmetic guarantees that bounding-box pruning never drops a row whose schema differs from the query's schema, and never returns a hit across mismatched schemas.</para>

		<sect2 xml:id="tpcbox_constructors">
			<title>Constructors</title>
			<itemizedlist>
				<listitem xml:id="tpcbox_tpcbox">
					<indexterm significance="normal"><primary><varname>tpcbox</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>tpcbox_z</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>tpcbox_t</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>tpcbox_xt</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>tpcbox_zt</varname></primary></indexterm>
					<para>Construct a tpcbox from explicit X/Y/Z/T bounds and a pcid</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
tpcbox(xmin,ymin,xmax,ymax,pcid,srid integer=0) → tpcbox
tpcbox_z(xmin,ymin,zmin,xmax,ymax,zmax,pcid,srid integer=0) → tpcbox
tpcbox_t(period,pcid) → tpcbox
tpcbox_xt(xmin,ymin,xmax,ymax,period,pcid,srid integer=0) → tpcbox
tpcbox_zt(xmin,ymin,zmin,xmax,ymax,zmax,period,pcid,srid integer=0) → tpcbox
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT tpcbox(0, 0, 10, 10, 1, 0);
-- TPCBOX(X((0,0),(10,10)), 1)
SELECT tpcbox_zt(0, 0, 0, 10, 10, 10, tstzspan '[2024-01-01, 2024-01-02]', 1, 0);
-- TPCBOX(ZT(((0,0,0),(10,10,10)),[2024-01-01, 2024-01-02]), 1)
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcbox_conversions">
			<title>Conversions</title>
			<itemizedlist>
				<listitem xml:id="tpcbox_tpcbox_pcpoint">
					<indexterm significance="normal"><primary><varname>tpcbox</varname></primary></indexterm>
					<para>Convert a pcpoint to a degenerate (zero-extent) tpcbox; SRID and pcid come from the schema</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
tpcbox(pcpoint) → tpcbox
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT tpcbox(pcpoint(1, 10.0, 20.0, 30.0));
-- TPCBOX(Z((10,20,30),(10,20,30)), 1)
</programlisting>
				</listitem>

				<listitem xml:id="tpcbox_tpcbox_pcpatch">
					<indexterm significance="normal"><primary><varname>tpcbox</varname></primary></indexterm>
					<para>Convert a pcpatch to a tpcbox using the patch's embedded 2D PCBOUNDS; the second form takes an explicit SRID instead of looking it up from the schema</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
tpcbox(pcpatch) → tpcbox
tpcbox(pcpatch,integer) → tpcbox
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT tpcbox(pcpatch(pcpoint(1, 10.0, 20.0, 30.0)));
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcbox_accessors">
			<title>Accessors</title>
			<itemizedlist>
				<listitem xml:id="tpcbox_hasX">
					<indexterm significance="normal"><primary><varname>hasX</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>hasZ</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>hasT</varname></primary></indexterm>
					<para>Return whether a tpcbox has the X/Y, Z, or T dimensions set</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
hasX(tpcbox) → boolean
hasZ(tpcbox) → boolean
hasT(tpcbox) → boolean
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
WITH b AS (SELECT tpcbox(0, 0, 10, 10, 1, 0) AS b)
  SELECT hasX(b), hasZ(b) FROM b;
-- t | f
</programlisting>
				</listitem>

				<listitem xml:id="tpcbox_xmin">
					<indexterm significance="normal"><primary><varname>xmin</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>xmax</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>ymin</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>ymax</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>zmin</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>zmax</varname></primary></indexterm>
					<para>Return a coordinate bound, NULL if the corresponding dimension is absent</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
xmin(tpcbox) → float
xmax(tpcbox) → float
ymin(tpcbox) → float
ymax(tpcbox) → float
zmin(tpcbox) → float
zmax(tpcbox) → float
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
WITH b AS (SELECT tpcbox(0, 0, 10, 10, 1, 0) AS b)
  SELECT xmin(b), xmax(b) FROM b;
-- 0 | 10
</programlisting>
				</listitem>

				<listitem xml:id="tpcbox_tmin">
					<indexterm significance="normal"><primary><varname>tmin</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>tmax</varname></primary></indexterm>
					<para>Return a temporal bound, NULL if the box has no T dimension</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
tmin(tpcbox) → timestamptz
tmax(tpcbox) → timestamptz
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT tmin(tpcbox_t(tstzspan '[2024-01-01, 2024-01-02]', 1));
-- 2024-01-01
</programlisting>
				</listitem>

				<listitem xml:id="tpcbox_pcid">
					<indexterm significance="normal"><primary><varname>pcid</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>SRID</varname></primary></indexterm>
					<para>Return the schema id and SRID</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
pcid(tpcbox) → integer
SRID(tpcbox) → integer
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
WITH b AS (SELECT tpcbox(0, 0, 10, 10, 7, 4326) AS b)
  SELECT pcid(b), SRID(b) FROM b;
-- 7 | 4326
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcbox_transformations">
			<title>Transformations</title>
			<itemizedlist>
				<listitem xml:id="tpcbox_round">
					<indexterm significance="normal"><primary><varname>round</varname></primary></indexterm>
					<para>Return a tpcbox with coordinates rounded to a given number of decimal digits</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
round(tpcbox,integer=0) → tpcbox
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT round(tpcbox(0.123456, 1.234567, 2.345678, 3.456789, 1, 0), 2);
-- TPCBOX(X((0.12,1.23),(2.35,3.46)), 1)
</programlisting>
				</listitem>

				<listitem xml:id="tpcbox_setSRID">
					<indexterm significance="normal"><primary><varname>setSRID</varname></primary></indexterm>
					<para>Return a tpcbox with the SRID overwritten</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
setSRID(tpcbox,integer) → tpcbox
</programlisting>
					<para>Does not reproject coordinates, for that, project the underlying tpcpoint first and take the bbox of the result</para>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT SRID(setSRID(tpcbox(0, 0, 10, 10, 1, 0), 4326));
-- 4326
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcbox_setops">
			<title>Set Operations</title>
			<itemizedlist>
				<listitem xml:id="tpcbox_union">
					<indexterm significance="normal"><primary><varname>+</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>*</varname></primary></indexterm>
					<para>Union and intersection of two tpcboxes</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
tpcbox {+, *} tpcbox → tpcbox
</programlisting>
					<para>Both operands must share the same <varname>pcid</varname>, and the intersection is NULL when the two boxes are disjoint.</para>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT tpcbox(0, 0, 5, 5, 1, 0) + tpcbox(3, 3, 10, 10, 1, 0);
-- TPCBOX(X((0,0),(10,10)), 1)
SELECT tpcbox(0, 0, 5, 5, 1, 0) * tpcbox(3, 3, 10, 10, 1, 0);
-- TPCBOX(X((3,3),(5,5)), 1)
</programlisting>
				</listitem>
			</itemizedlist>
			<para>Note: a generic box-vs-box <varname>minus</varname> is not provided, mirroring the convention for <varname>stbox</varname> and <varname>tbox</varname>: the difference of two boxes is not generally a box.</para>
		</sect2>

		<sect2 xml:id="tpcbox_topo">
			<title>Topological Operations</title>
			<para>All topological operators take two <varname>tpcbox</varname> values with matching <varname>pcid</varname>. A pcid mismatch always yields false (not an error) so that index scans behave intuitively.</para>
			<itemizedlist>
				<listitem xml:id="tpcbox_topo_ops">
					<indexterm significance="normal"><primary><varname>&amp;&amp;</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>@&gt;</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&lt;@</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>~=</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>-|-</varname></primary></indexterm>
					<para>Topological operators</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
tpcbox {&amp;&amp;, @&gt;, &lt;@, ~=, -|-} tpcbox → boolean
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT tpcbox(0, 0, 10, 10, 1, 0) @&gt; tpcbox(2, 2, 8, 8, 1, 0);
-- t
SELECT tpcbox(2, 2, 8, 8, 1, 0) &lt;@ tpcbox(0, 0, 10, 10, 1, 0);
-- t
SELECT tpcbox(0, 0, 5, 5, 1, 0) &amp;&amp; tpcbox(3, 3, 10, 10, 1, 0);
-- t
SELECT tpcbox(0, 0, 2, 2) ~= tpcbox(0, 0, 2, 2);
-- t
SELECT tpcbox(0, 0, 2, 2) -|- tpcbox(5, 5, 7, 7);
-- f
SELECT tpcbox(0, 0, 5, 5, 1, 0) &amp;&amp; tpcbox(0, 0, 5, 5, 2, 0);
-- ERROR:  Operation on TPCBox values with different schemas: 1 vs 2
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcbox_pos">
			<title>Position Operations</title>
			<para>Sixteen axis-aligned position predicates, parallel to those on <varname>stbox</varname>. Each tests strict-or-overlap relationship along one axis. All require matching <varname>pcid</varname>; mismatch returns false. Z-axis predicates require both boxes to have a Z dimension; T-axis predicates require both to have a T dimension.</para>
			<itemizedlist>
				<listitem xml:id="tpcbox_pos_ops">
					<indexterm significance="normal"><primary><varname>&lt;&lt;</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&amp;&lt;</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&gt;&gt;</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&amp;&gt;</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&lt;&lt;|</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&amp;&lt;|</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>|&gt;&gt;</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>|&amp;&gt;</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&lt;&lt;/</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&amp;&lt;/</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>/&gt;&gt;</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>/&amp;&gt;</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&lt;&lt;#</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&amp;&lt;#</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>#&gt;&gt;</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>#&amp;&gt;</varname></primary></indexterm>
					<para>Position operators</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
tpcbox {&lt;&lt;, &amp;&lt;, &gt;&gt;, &amp;&gt;} tpcbox → boolean
tpcbox {&lt;&lt;|, &amp;&lt;|, |&gt;&gt;, |&amp;&gt;} tpcbox → boolean
tpcbox {&lt;&lt;/, &amp;&lt;/, /&gt;&gt;, /&amp;&gt;} tpcbox → boolean
tpcbox {&lt;&lt;#, &amp;&lt;#, #&gt;&gt;, #&amp;&gt;} tpcbox → boolean
</programlisting>
					<para>The four groups test the X, the Y, the Z and the time axis, and each states, in order, strictly before, does not extend after, strictly after, and does not extend before along its axis.</para>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT tpcbox(0, 0, 2, 2) &lt;&lt; tpcbox(5, 5, 7, 7);
-- t
SELECT tpcbox(0, 0, 2, 2) &lt;&lt;| tpcbox(5, 5, 7, 7);
-- t
-- Neither box carries a z dimension or a time span, so those tests are false.
SELECT tpcbox(0, 0, 2, 2) &lt;&lt;/ tpcbox(5, 5, 7, 7);
-- f
SELECT tpcbox(0, 0, 2, 2) &lt;&lt;# tpcbox(5, 5, 7, 7);
-- f
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcbox_comparisons">
			<title>Comparisons</title>
			<itemizedlist>
				<listitem xml:id="tpcbox_btree">
					<indexterm significance="normal"><primary><varname>=</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&lt;&gt;</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&lt;</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&lt;=</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&gt;=</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&gt;</varname></primary></indexterm>
					<para>Compare two tpcboxes; total order over the canonical encoding (pcid, srid, flags, period, then spatial bounds in XYZ-min/max order)</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
tpcbox {=, &lt;&gt;, &lt;, &lt;=, &gt;, &gt;=} tpcbox → boolean
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT tpcbox(0, 0, 2, 2) = tpcbox(0, 0, 2, 2);
-- t
SELECT tpcbox(0, 0, 2, 2) &lt;&gt; tpcbox(5, 5, 7, 7);
-- t
SELECT tpcbox(0, 0, 2, 2) &lt; tpcbox(5, 5, 7, 7);
-- t
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>
	</sect1>

	<sect1 xml:id="tpcpoint">
		<title>Temporal Point Cloud Points</title>

		<para>A <varname>tpcpoint</varname> is the lifting of <varname>pcpoint</varname> through MobilityDB's temporal machinery. It supports the same three subtypes as the other temporal types, instant, sequence (with discrete or step interpolation), and sequence set. All instants in a single <varname>tpcpoint</varname> value must share the same <varname>pcid</varname>; the constructor enforces this.</para>

		<para>The bounding box of a <varname>tpcpoint</varname> is a <varname>tpcbox</varname>, computed at construction time from the X/Y/Z dimensions of the underlying <varname>pcpoint</varname> values together with the timestamps of the instants.</para>

		<para>A column or domain may pin the schema by adding the <varname>pcid</varname> as a single-integer typmod, exactly like PostGIS' <varname>geometry(Point, SRID)</varname>. Mixing schemas in such a column is rejected at INSERT or cast time. The same construct applies to <varname>tpcpatch</varname>.</para>
		<programlisting language="sql" xml:space="preserve" format="linespecific">
CREATE TABLE scans (id int, traj tpcpoint(1), full_scan tpcpatch(1));
-- Accepts pcid 1
INSERT INTO scans VALUES (1,
  tpcpoint(pcpoint(1, 1.0, 2.0, 3.0), '2024-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2)), '2024-01-01'::timestamptz));
-- Rejects any other pcid:
-- ERROR: Pcid of tpcpoint value (2) does not match column typmod pcid (1)
</programlisting>
		<para>Omitting the typmod (<varname>tpcpoint</varname> with no parenthesised pcid) leaves the column schema-agnostic. The runtime check in the <varname>extent</varname> aggregate still rejects mid-query mixed-pcid input even on unconstrained columns.</para>

		<sect2 xml:id="tpcpoint_inout">
			<title>Input and Output</title>
			<itemizedlist>
				<listitem xml:id="tpcpoint_asText">
					<indexterm significance="normal"><primary><varname>asText</varname></primary></indexterm>
					<para>Return the text representation of a tpcpoint, or of an array of them, and cast a tpcpoint to text</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
asText(tpcpoint) → text
asText(tpcpoint[]) → text[]
tpcpoint::text → text
</programlisting>
								<programlisting language="sql" xml:space="preserve" format="linespecific">
-- The pcpoint payload renders as hex-encoded WKB.
SELECT asText(tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz));
-- 5C00000001000000640000006400000064000000000000@2001-01-01
</programlisting>
				</listitem>
				<listitem xml:id="tpcpoint_asMFJSON">
					<indexterm significance="normal"><primary><varname>asMFJSON</varname></primary></indexterm>
					<para>For <varname>tpcpoint</varname>, the temporal type is emitted as <varname>{"type":"MovingPCPoint", … "coordinates":[[X,Y,Z], …], "datetimes":[…]}</varname></para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
asMFJSON(tpcpoint,options int=0,flags int=0,maxdecimaldigits int=15) → text
</programlisting>
					<para>X/Y/[Z] are extracted from each instant's <varname>pcpoint</varname> via the schema cache; if the schema lacks X/Y the coordinate array is empty for that instant.</para>
								<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT asMFJSON(tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz));
/* {"type":"MovingPCPoint","coordinates":[[1,1,1]],
   "datetimes":["2001-01-01T00:00:00"],"interpolation":"None"} */
</programlisting>
				</listitem>
			</itemizedlist>
			<para><varname>asText</varname> takes no <varname>maxdecimaldigits</varname> argument, a <varname>pcpoint</varname> printing as the hex-WKB of its serialized form, which has no decimal places to round, and there is no <varname>tpcpointFromText</varname>, pgPointCloud's <varname>pcpoint_in</varname> accepting hex-WKB only and the schema not being reconstructible from the text form. MF-JSON is output-only for the same reason, and carries a <varname>"bbox"</varname> key when <varname>options</varname> is 1, a TPCBox JSON object adding a <varname>"pcid"</varname> field next to the standard stbox-shaped <varname>"bbox"</varname> array. For a lossless round trip use <varname>asBinary</varname> / <varname>tpcpointFromBinary</varname> or <varname>asHexWKB</varname> / <varname>tpcpointFromHexWKB</varname>.</para>
		</sect2>

		<sect2 xml:id="tpcpoint_constructors">
			<title>Constructors</title>
			<itemizedlist>
				<listitem xml:id="tpcpoint_tpcpoint">
					<indexterm significance="normal"><primary><varname>tpcpoint</varname></primary></indexterm>
					<para>Construct a temporal pcpoint of instant subtype</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
tpcpoint(pcpoint,timestamptz) → tpcpoint
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT tpcpoint(pcpoint(1, 10.0, 20.0, 30.0), '2024-01-01 12:00+00');
</programlisting>
				</listitem>

				<listitem xml:id="tpcpoint_tpcpointSeq">
					<indexterm significance="normal"><primary><varname>tpcpointSeq</varname></primary></indexterm>
					<para>Construct a temporal pcpoint of sequence subtype from an array of instants</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
tpcpointSeq(tpcpoint[]) → tpcpoint
tpcpointSeq(tpcpoint[],text) → tpcpoint
</programlisting>
					<para>The interpolation is one of <varname>'discrete'</varname>, <varname>'step'</varname>; <varname>'step'</varname> is the default for tpcpoint since pcpoints carry heterogeneous dimensions that do not interpolate linearly</para>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT tpcpointSeq(ARRAY[tpcpoint(pcpoint(1, 1.0, 2.0, 3.0), '2024-01-01'::timestamptz),
  tpcpoint(pcpoint(1, 4.0, 5.0, 6.0), '2024-01-02'::timestamptz)], 'step');
</programlisting>
				</listitem>

				<listitem xml:id="tpcpoint_tpcpointSeqSet">
					<indexterm significance="normal"><primary><varname>tpcpointSeqSet</varname></primary></indexterm>
					<para>Construct a temporal pcpoint of sequence-set subtype from an array of sequences</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
tpcpointSeqSet(tpcpoint[]) → tpcpoint
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT numSequences(tpcpointSeqSet(ARRAY[tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz),
  tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz)])]));
-- 1
SELECT numInstants(tpcpointSeqSet(ARRAY[tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz),
  tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz)])]));
-- 2
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcpoint_conversions">
			<title>Conversions</title>
			<itemizedlist>
				<listitem xml:id="tpcpoint_tgeompoint">
					<indexterm significance="normal"><primary><varname>::</varname></primary></indexterm>
					<para>Cast a tpcpoint to a temporal geometry point, projecting away every non-spatial dimension while preserving timestamps</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
tpcpoint::tgeompoint
</programlisting>
					<para><varname>tgeompoint::tpcpoint</varname> is not defined</para>
					<para>The result has the same SRID as the schema; its dimensionality (2D vs. 3D) follows the presence of Z in the schema. A reverse cast is not provided: reconstructing a pcpoint requires a target schema that the temporal value alone cannot supply</para>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT ST_AsText(startValue(tpcpoint(pcpoint(1, 10.0, 20.0, 30.0),
  '2024-01-01'::timestamptz)::tgeompoint));
-- POINT Z (10 20 30)
</programlisting>
				</listitem>

			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcpoint_accessors">
			<title>Accessors</title>
			<para>All generic temporal accessors apply (see <xref linkend="ttype_p1" />): <varname>numInstants</varname>, <varname>startInstant</varname>, <varname>endInstant</varname>, <varname>instantN</varname>, <varname>instants</varname>, <varname>startValue</varname>, <varname>endValue</varname>, <varname>getValues</varname>, <varname>numTimestamps</varname>, <varname>startTimestamp</varname>, <varname>endTimestamp</varname>, <varname>getTime</varname>, <varname>duration</varname>, <varname>memSize</varname>, <varname>interp</varname>. In addition:</para>
			<itemizedlist>
				<listitem xml:id="tpcpoint_pcid">
					<indexterm significance="normal"><primary><varname>pcid</varname></primary></indexterm>
					<para>Return the pgPointCloud schema id shared by all instants</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
pcid(tpcpoint) → integer
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT pcid(tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz));
-- 1
</programlisting>
				</listitem>

				<listitem xml:id="tpcpoint_SRID">
					<indexterm significance="normal"><primary><varname>SRID</varname></primary></indexterm>
					<para>Return the SRID inherited from the schema</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
SRID({tpcpoint,tpcpatch}) → integer
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
-- The SRID is that of the schema registered for the pcid.
SELECT SRID(tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz));
-- 0
SELECT SRID(tpcpatch(pcpatch(pcpoint(1, 1, 1, 1)), '2001-01-01'::timestamptz));
-- 0
</programlisting>
				</listitem>

				<listitem xml:id="tpcpoint_getX">
					<indexterm significance="normal"><primary><varname>getX</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>getY</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>getZ</varname></primary></indexterm>
					<para>Project a tpcpoint onto a spatial dimension, yielding a tfloat</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
getX(tpcpoint) → tfloat
getY(tpcpoint) → tfloat
getZ(tpcpoint) → tfloat
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT startValue(getX(tpcpoint(pcpoint(1, 10.0, 20.0, 30.0),
  '2024-01-01'::timestamptz)));
-- 10
</programlisting>
				</listitem>

				<listitem xml:id="tpcpoint_getDim">
					<indexterm significance="normal"><primary><varname>getDim</varname></primary></indexterm>
					<para>Project a tpcpoint onto any named schema dimension, yielding a tfloat</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
getDim(tpcpoint,text) → tfloat
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT getDim(tpcpointSeq(ARRAY[tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz),
  tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz)]), 'X');
-- Interp=Step;[1@2001-01-01, 2@2001-01-02]
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcpoint_transformations">
			<title>Transformations</title>
			<itemizedlist>
				<listitem xml:id="tpcpoint_tpcpointInst">
					<indexterm significance="normal"><primary><varname>tpcpointInst</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>tpcpointSeq</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>tpcpointSeqSet</varname></primary></indexterm>
					<para>Transform a tpcpoint to another subtype</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
tpcpointInst(tpcpoint) → tpcpoint
tpcpointSeq(tpcpoint,interp text='step') → tpcpoint
tpcpointSeqSet(tpcpoint) → tpcpoint
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT numInstants(tpcpointInst(tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz)));
-- 1
SELECT numInstants(tpcpointSeq(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz),
  tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz)])));
-- 2
</programlisting>
				</listitem>
				<listitem xml:id="tpcpoint_setInterp">
					<indexterm significance="normal"><primary><varname>setInterp</varname></primary></indexterm>
					<para>Transform a tpcpoint to another interpolation</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
setInterp(tpcpoint,interp) → tpcpoint
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
-- A tpcpoint carries step interpolation.
SELECT interp(tpcpointSeq(ARRAY[tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz),
  tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz)]));
-- Step
SELECT interp(setInterp(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz), tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz)],
  'discrete'), 'step'));
-- Step
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcpoint_modifications">
			<title>Modifications</title>
			<itemizedlist>
				<listitem xml:id="tpcpoint_insert">
					<indexterm significance="normal"><primary><varname>insert</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>update</varname></primary></indexterm>
					<para>Insert a tpcpoint into another one, or update another one with it</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
insert(tpcpoint,tpcpoint,connect boolean=true) → tpcpoint
update(tpcpoint,tpcpoint,connect boolean=true) → tpcpoint
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT numInstants(insert(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz), tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz)]),
  tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)])));
-- 3
</programlisting>
				</listitem>

				<listitem xml:id="tpcpoint_deleteTime">
					<indexterm significance="normal"><primary><varname>deleteTime</varname></primary></indexterm>
					<para>Delete a time selector (timestamp, set, period, or spanset) from a tpcpoint</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
deleteTime(tpcpoint,time,connect boolean=true) → tpcpoint
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
-- Deleting the middle instant splits the sequence into {[2001-01-01, 2001-01-02),
-- (2001-01-02, 2001-01-03]}; each half stores its bound at the deleted timestamp, giving
-- four instants.
SELECT numInstants(deleteTime(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz), tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)]), timestamptz '2001-01-02'));
-- 4
</programlisting>
				</listitem>

				<listitem xml:id="tpcpoint_appendInstant">
					<indexterm significance="normal"><primary><varname>appendInstant</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>appendSequence</varname></primary></indexterm>
					<para>Append an instant or a sequence to a tpcpoint</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
appendInstant(tpcpoint,tpcpoint) → tpcpoint
appendSequence(tpcpoint,tpcpoint) → tpcpoint
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT numInstants(appendInstant(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz), tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz)]),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)));
-- 3
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcpoint_restrictions">
			<title>Restrictions</title>
			<itemizedlist>
				<listitem xml:id="tpcpoint_atValue">
					<indexterm significance="normal"><primary><varname>atValue</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>minusValue</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>atValues</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>minusValues</varname></primary></indexterm>
					<para>Restrict a tpcpoint to (the complement of) a pcpoint value or a set of pcpoint values</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
atValue(tpcpoint,pcpoint) → tpcpoint
minusValue(tpcpoint,pcpoint) → tpcpoint
atValues(tpcpoint,pcpointset) → tpcpoint
minusValues(tpcpoint,pcpointset) → tpcpoint
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
-- With step interpolation the matching value is held on [2001-01-01, 2001-01-02); its
-- closing bound is stored as a second instant.
SELECT numInstants(atValue(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz), tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)]), pcpoint(1, 1, 1, 1)));
-- 2
SELECT numInstants(minusValue(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz), tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)]), pcpoint(1, 1, 1, 1)));
-- 2
</programlisting>
				</listitem>

				<listitem xml:id="tpcpoint_atTime">
					<indexterm significance="normal"><primary><varname>atTime</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>minusTime</varname></primary></indexterm>
					<para>Restrict a tpcpoint to a time selector (timestamp, period, set, or spanset), or remove it</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
atTime(tpcpoint,time) → tpcpoint
minusTime(tpcpoint,time) → tpcpoint
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT numInstants(atTime(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz), tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)]), timestamptz '2001-01-02'));
-- 1
SELECT numInstants(atTime(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz), tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)]),
  tstzspan '[2001-01-01, 2001-01-02]'));
-- 2
</programlisting>
				</listitem>

				<listitem xml:id="tpcpoint_atTpcbox">
					<indexterm significance="normal"><primary><varname>atTpcbox</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>minusTpcbox</varname></primary></indexterm>
					<para>Restrict a tpcpoint to the spatial / temporal extent of a tpcbox, or remove it</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
atTpcbox(tpcpoint,tpcbox,border_inc boolean=true) → tpcpoint
minusTpcbox(tpcpoint,tpcbox,border_inc boolean=true) → tpcpoint
</programlisting>
					<para>Returns NULL when the tpcbox's pcid does not match the tpcpoint's. Internally projects to a tgeompoint via the schema cache, restricts the projection by the equivalent stbox, and lifts the result's time span back onto the original tpcpoint to preserve the full pcpoint values</para>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT numInstants(atTpcbox(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz), tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)]),
  tpcbox_zt(0, 0, 0, 2, 2, 2, tstzspan '[2001-01-01, 2001-01-03]', 1)));
-- 2
-- The box does not intersect the tpcpoint, so nothing is removed.
SELECT numInstants(minusTpcbox(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz), tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)]),
  tpcbox_zt(10, 10, 10, 12, 12, 12, tstzspan '[2001-01-01, 2001-01-03]', 1)));
-- 3
</programlisting>
				</listitem>

				<listitem xml:id="tpcpoint_beforeTimestamp">
					<indexterm significance="normal"><primary><varname>beforeTimestamp</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>afterTimestamp</varname></primary></indexterm>
					<para>Restrict a tpcpoint to the instants before or after a timestamp</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
beforeTimestamp(tpcpoint,timestamptz,strict boolean=true) → tpcpoint
afterTimestamp(tpcpoint,timestamptz,strict boolean=true) → tpcpoint
</programlisting>
					<para>The strict flag excludes the instant at the timestamp itself</para>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT timeSpan(beforeTimestamp(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz), tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)]), timestamptz '2001-01-02'));
-- [2001-01-01, 2001-01-02)
SELECT timeSpan(afterTimestamp(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz), tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)]), timestamptz '2001-01-02'));
-- (2001-01-02, 2001-01-03]
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcpoint_splitting">
			<title>Splitting Operations</title>
			<itemizedlist>
				<listitem xml:id="tpcpoint_unnest">
					<indexterm significance="normal"><primary><varname>unnest</varname></primary></indexterm>
					<para>Return the distinct values of a tpcpoint with the span set on which each of them is taken</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
unnest(tpcpoint) → {(value,time)}
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT (un).time FROM (SELECT unnest(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2024-01-01'::timestamptz), tpcpoint(pcpoint(1, 2, 2, 2), '2024-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 1, 1, 1), '2024-01-03'::timestamptz)])) AS un) t;
-- {[2024-01-01, 2024-01-02), [2024-01-03, 2024-01-03]}
-- {[2024-01-02, 2024-01-03)}
</programlisting>
				</listitem>

				<listitem xml:id="tpcpoint_timeSplit">
					<indexterm significance="normal"><primary><varname>timeSplit</varname></primary></indexterm>
					<para>Split a tpcpoint into fragments, one per time bin</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
timeSplit(tpcpoint,bin_width interval,
  origin timestamptz='2000-01-03') → {(time,tpcpoint)}
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT (ts).time, numInstants((ts).temp) FROM (SELECT timeSplit(tpcpointSeq(ARRAY[
  tpcpoint(pcpoint(1, 1, 1, 1), '2024-01-01'::timestamptz),
  tpcpoint(pcpoint(1, 2, 2, 2), '2024-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2024-01-03'::timestamptz)]),
  interval '1 day', '2024-01-01'::timestamptz) AS ts) t;
-- 2024-01-01 | 2
-- 2024-01-02 | 2
-- 2024-01-03 | 1
</programlisting>
				</listitem>

				<listitem xml:id="tpcpoint_spans">
					<indexterm significance="normal"><primary><varname>spans</varname></primary></indexterm>
					<para>Return the array of time spans of a tpcpoint's segments</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
spans(tpcpoint) → tstzspan[]
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT spans(tpcpointSeq(ARRAY[tpcpoint(pcpoint(1, 1, 1, 1), '2024-01-01'::timestamptz),
  tpcpoint(pcpoint(1, 2, 2, 2), '2024-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2024-01-03'::timestamptz)]));
-- {"[2024-01-01, 2024-01-02]","[2024-01-02, 2024-01-03]"}
</programlisting>
				</listitem>

				<listitem xml:id="tpcpoint_splitNSpans">
					<indexterm significance="normal"><primary><varname>splitNSpans</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>splitEachNSpans</varname></primary></indexterm>
					<para>Return the time spans of a tpcpoint split into a given number of bins, or with a given number of segments per bin</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
splitNSpans(tpcpoint,integer) → tstzspan[]
splitEachNSpans(tpcpoint,integer) → tstzspan[]
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT splitNSpans(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2024-01-01'::timestamptz), tpcpoint(pcpoint(1, 2, 2, 2), '2024-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2024-01-03'::timestamptz)]), 2);
-- {"[2024-01-01, 2024-01-02]","[2024-01-02, 2024-01-03]"}
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcpoint_bbox_ops">
			<title>Bounding Box Operations</title>
			<para>The bbox operators evaluate against the value's <varname>tpcbox</varname>; see <xref linkend="tpcbox_topo" /> and <xref linkend="tpcbox_pos" /> for the per-operator geometry. Each operator below is wired against <varname>tpcbox</varname>, <varname>tstzspan</varname>, and <varname>tpcpoint</varname> itself.</para>
			<itemizedlist>
				<listitem xml:id="tpcpoint_topo">
					<indexterm significance="normal"><primary><varname>&amp;&amp;</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>@&gt;</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&lt;@</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>~=</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>-|-</varname></primary></indexterm>
					<para>Topological operators</para>
								<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT tpcbox 'TPCBOX(ZT(((0,0,0),(2,2,2)),[2024-01-01, 2024-01-03]), 1)' &amp;&amp;
  tpcbox 'TPCBOX(ZT(((1,1,1),(3,3,3)),[2024-01-02, 2024-01-04]), 1)';
-- true
SELECT tpcbox 'TPCBOX(ZT(((0,0,0),(4,4,4)),[2024-01-01, 2024-01-04]), 1)' @&gt;
  tpcbox 'TPCBOX(ZT(((1,1,1),(2,2,2)),[2024-01-02, 2024-01-03]), 1)';
-- true
SELECT tpcbox 'TPCBOX(ZT(((0,0,0),(2,2,2)),[2024-01-01, 2024-01-03]), 1)' ~=
  tpcbox 'TPCBOX(ZT(((0,0,0),(2,2,2)),[2024-01-01, 2024-01-03]), 1)';
-- true
SELECT tpcbox 'TPCBOX(ZT(((0,0,0),(2,2,2)),[2024-01-01, 2024-01-02)), 1)' -|-
  tpcbox 'TPCBOX(ZT(((0,0,0),(2,2,2)),[2024-01-02, 2024-01-03]), 1)';
-- true
</programlisting>
			</listitem>
				<listitem xml:id="tpcpoint_pos">
					<indexterm significance="normal"><primary><varname>&lt;&lt;</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&gt;&gt;</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&lt;&lt;|</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>|&gt;&gt;</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&lt;&lt;/</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>/&gt;&gt;</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&lt;&lt;#</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>#&gt;&gt;</varname></primary></indexterm>
					<para>Position operators</para>
								<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT tpcbox 'TPCBOX(ZT(((0,0,0),(1,1,1)),[2024-01-01, 2024-01-02]), 1)' &lt;&lt;
  tpcbox 'TPCBOX(ZT(((5,5,5),(6,6,6)),[2024-01-03, 2024-01-04]), 1)';
-- true
SELECT tpcbox 'TPCBOX(ZT(((0,0,0),(1,1,1)),[2024-01-01, 2024-01-02]), 1)' &lt;&lt;|
  tpcbox 'TPCBOX(ZT(((5,5,5),(6,6,6)),[2024-01-03, 2024-01-04]), 1)';
-- true
SELECT tpcbox 'TPCBOX(ZT(((0,0,0),(1,1,1)),[2024-01-01, 2024-01-02]), 1)' &lt;&lt;/
  tpcbox 'TPCBOX(ZT(((5,5,5),(6,6,6)),[2024-01-03, 2024-01-04]), 1)';
-- true
SELECT tpcbox 'TPCBOX(ZT(((0,0,0),(1,1,1)),[2024-01-01, 2024-01-02]), 1)' &lt;&lt;#
  tpcbox 'TPCBOX(ZT(((5,5,5),(6,6,6)),[2024-01-03, 2024-01-04]), 1)';
-- true
</programlisting>
			</listitem>
			</itemizedlist>
		</sect2>
		<sect2 xml:id="tpcpoint_distance_ops">
				<title>Distance Operations</title>
				<itemizedlist>
				<listitem xml:id="tpcpoint_nearestApproachDistance">
					<indexterm significance="normal"><primary><varname>|=|</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>nearestApproachDistance</varname></primary></indexterm>
					<para>Return the smallest distance ever</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
nearestApproachDistance(tpcpoint,{tpcbox,tpcpoint}) → float
</programlisting>
					<para>Pcid mismatch yields infinity.</para>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT round(nearestApproachDistance( tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz),
  tpcpoint(pcpoint(1, 10, 10, 10), '2001-01-01'::timestamptz))::numeric, 4);
-- 15.5885
-- A tpcbox without a time span measures the spatial approach only.
SELECT round(nearestApproachDistance(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz), tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)]),
  tpcbox_z(10, 10, 10, 12, 12, 12, 1))::numeric, 4);
-- 12.1244
SELECT round((tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz) |=|
  tpcpoint(pcpoint(1, 10, 10, 10), '2001-01-01'::timestamptz))::numeric, 4);
-- 15.5885
</programlisting>
				</listitem>
				</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcpoint_spatialrels">
			<title>Ever and Always Relationships</title>
			<para>Evaluated by projecting the tpcpoint to a <varname>tgeompoint</varname> via the schema cache and delegating to the corresponding tgeompoint relation. Each function is wired in three argument orders: <varname>(geometry, tpcpoint)</varname>, <varname>(tpcpoint, geometry)</varname>, and <varname>(tpcpoint, tpcpoint)</varname>. The ever/always relationships are wired for tpcpoint alone; a tpcpatch answers the temporal relationships instead, reading a patch as the multipoint of the positions its points occupy. A tpcpoint declares the relationships its cast target declares, so <varname>eContains</varname>, <varname>eCovers</varname> and <varname>eTouches</varname> and their always twins are wired as well: a schema whose dimensions do not include Z answers them, and one carrying Z meets the planar refusal <varname>The tgeompoint cannot have Z dimension</varname>, exactly as a three-dimensional temporal geometry point does.</para>
			<itemizedlist>
				<listitem xml:id="tpcpoint_eContains">
					<indexterm significance="normal"><primary><varname>eContains</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>aContains</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>eCovers</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>aCovers</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>eTouches</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>aTouches</varname></primary></indexterm>
					<para>Ever / always contains, covers and touches</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
eContains(geometry,tpcpoint) &#8594; boolean
aContains(geometry,tpcpoint) &#8594; boolean
eCovers(geometry,tpcpoint) &#8594; boolean
aCovers(geometry,tpcpoint) &#8594; boolean
eTouches(geometry,tpcpoint) &#8594; boolean
aTouches(geometry,tpcpoint) &#8594; boolean
eTouches(tpcpoint,geometry) &#8594; boolean
aTouches(tpcpoint,geometry) &#8594; boolean
</programlisting>
					<para>A moving point neither contains nor covers a geometry and two moving points do not touch, so these take the geometry first only, apart from <varname>eTouches</varname> and <varname>aTouches</varname>, which read either order. They are planar relationships: a point whose schema states a Z dimension is refused.</para>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT eContains(geometry 'Polygon((0 0,0 4,4 4,4 0,0 0))',
  tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz));
-- ERROR:  The tgeompoint cannot have Z dimension
</programlisting>
				</listitem>

				<listitem xml:id="tpcpoint_eIntersects">
					<indexterm significance="normal"><primary><varname>eIntersects</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>aIntersects</varname></primary></indexterm>
					<para>Ever / always intersects</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
eIntersects(tpcpoint,{geometry,tpcpoint}) → boolean
aIntersects(tpcpoint,{geometry,tpcpoint}) → boolean
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT eIntersects(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz), tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)]), geometry 'POINT Z (1 1 1)');
-- t
SELECT aIntersects(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz), tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)]), geometry 'POINT Z (1 1 1)');
-- f
SELECT eIntersects(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz), tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)]),
  tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz),
  tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)]));
-- t
</programlisting>
				</listitem>
				<listitem xml:id="tpcpoint_eDisjoint">
					<indexterm significance="normal"><primary><varname>eDisjoint</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>aDisjoint</varname></primary></indexterm>
					<para>Ever / always disjoint</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
eDisjoint(tpcpoint,{geometry,tpcpoint}) → boolean
aDisjoint(tpcpoint,{geometry,tpcpoint}) → boolean
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT eDisjoint(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz), tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)]), geometry 'POINT Z (1 1 1)');
-- t
SELECT aDisjoint(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz), tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)]), geometry 'POINT Z (1 1 1)');
-- f
</programlisting>
				</listitem>
				<listitem xml:id="tpcpoint_eDwithin">
					<indexterm significance="normal"><primary><varname>eDwithin</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>aDwithin</varname></primary></indexterm>
					<para>Ever / always within distance</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
eDwithin(tpcpoint,{geometry,tpcpoint},float) → boolean
aDwithin(tpcpoint,{geometry,tpcpoint},float) → boolean
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT eDwithin(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz), tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)]), geometry 'POINT Z (5 5 5)',
  4);
-- t
SELECT aDwithin(tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz), tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)]), geometry 'POINT Z (5 5 5)',
  4);
-- f
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcpoint_spatiotemp_rels">
		<title>Spatiotemporal Relationships</title>
			<para>The spatiotemporal relationships compute the topological or distance relationship at each instant and result in a <varname>tbool</varname>. They read the same <varname>tgeompoint</varname> projection the ever/always relationships read, and are wired in the same three argument orders. A tpcpoint carries the Z its schema declares, if any: <varname>tContains</varname>, <varname>tCovers</varname> and <varname>tTouches</varname> are planar relationships, so a schema whose dimensions do not include Z answers them and one carrying Z is refused. That projection carries the interpolation of its source, so a tpcpoint sequence is answered along its trajectory rather than at its instants alone.</para>
			<itemizedlist>
				<listitem xml:id="tpcpoint_tDisjoint">
					<indexterm significance="normal"><primary><varname>tContains</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>tCovers</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>tDisjoint</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>tDwithin</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>tIntersects</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>tTouches</varname></primary></indexterm>
					<para>Temporal spatial relationships</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
tDisjoint({geometry,tpcpoint},{geometry,tpcpoint}) &#8594; tbool
tDwithin({geometry,tpcpoint},{geometry,tpcpoint},float) &#8594; tbool
tIntersects({geometry,tpcpoint},{geometry,tpcpoint}) &#8594; tbool
tContains(geometry,tpcpoint) &#8594; tbool
tCovers(geometry,tpcpoint) &#8594; tbool
tTouches(geometry,tpcpoint) &#8594; tbool
tTouches(tpcpoint,geometry) &#8594; tbool
</programlisting>
					<para>A temporal point cloud point declares the relationships its cast target declares, so <varname>tContains</varname> and <varname>tCovers</varname> take the geometry first only and <varname>tTouches</varname> has no direction between two point cloud points. The three are planar relationships: a schema whose dimensions do not include Z answers them, and one carrying Z meets the refusal <varname>The tgeompoint cannot have Z dimension</varname>.</para>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT tIntersects(tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz),
  geometry 'Polygon((0 0,0 2,2 2,2 0,0 0))');
-- t@2001-01-01
SELECT tDisjoint(tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz),
  geometry 'Polygon((0 0,0 2,2 2,2 0,0 0))');
-- f@2001-01-01
SELECT tDisjoint(tpcpoint(pcpoint(1, 9, 9, 9), '2001-01-02'::timestamptz),
  geometry 'Polygon((0 0,0 2,2 2,2 0,0 0))');
-- t@2001-01-02
SELECT tDisjoint(tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz),
  tpcpoint(pcpoint(1, 9, 9, 9), '2001-01-01'::timestamptz));
-- t@2001-01-01
SELECT tDwithin(tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz),
  geometry 'Point(1 2)', 2.0);
-- t@2001-01-01
SELECT tDwithin(tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz),
  geometry 'Point(1 5)', 2.0);
-- f@2001-01-01
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>
		<sect2 xml:id="tpcpoint_compops">
			<title>Comparisons</title>
			<itemizedlist>
				<listitem xml:id="tpcpoint_btree">
					<indexterm significance="normal"><primary><varname>=</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&lt;&gt;</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&lt;</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&lt;=</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&gt;=</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>&gt;</varname></primary></indexterm>
					<para>Traditional comparisons</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
tpcpoint {=, &lt;&gt;, &lt;, &lt;=, &gt;, &gt;=} tpcpoint → boolean
</programlisting>
					<para>The order is lexicographic over the canonical encodings of the two values, not geometric.</para>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz) = tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz);
-- t
SELECT tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz) &lt;&gt; tpcpoint(pcpoint(1, 2,
  2, 2), '2001-01-02'::timestamptz);
-- t
SELECT tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz) &lt; tpcpoint(pcpoint(1, 2,
  2, 2), '2001-01-02'::timestamptz);
-- t
</programlisting>
				</listitem>

				<listitem xml:id="tpcpoint_ever_eq">
					<indexterm significance="normal"><primary><varname>?=</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>%=</varname></primary></indexterm>
					<para>Ever and always comparisons</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
{pcpoint,tpcpoint} {?=, %=, ?&lt;&gt;, %&lt;&gt;} {pcpoint,tpcpoint} → boolean
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT pcpoint(1, 1, 1,
  1) ?= tpcpointSeq(ARRAY[ tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz),
  tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)]);
-- t
SELECT tpcpointSeq(ARRAY[tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz),
  tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)]) %= pcpoint(1, 1, 1, 1);
-- f
</programlisting>
				</listitem>

				<listitem xml:id="tpcpoint_temporal_eq">
					<indexterm significance="normal"><primary><varname>#=</varname></primary></indexterm>
					<para>Temporal comparisons</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
{pcpoint,tpcpoint} {#=, #&lt;&gt;} {pcpoint,tpcpoint} → tbool
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT tpcpointSeq(ARRAY[tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz),
  tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz),
  tpcpoint(pcpoint(1, 3, 3, 3), '2001-01-03'::timestamptz)]) #= pcpoint(1, 1, 1, 1);
-- [t@2001-01-01, f@2001-01-02, f@2001-01-03]
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

	</sect1>

	<sect1 xml:id="tpcpatch">
		<title>Temporal Point Cloud Patches</title>

		<para>A <varname>tpcpatch</varname> is the lifting of <varname>pcpatch</varname>. It mirrors <varname>tpcpoint</varname> in every regard, same subtypes, same accessors, same casts and operators, except that each instant carries an entire compressed batch of points instead of a single point. The bounding box is again a <varname>tpcbox</varname>, computed in O(1) per instant from the patch's embedded <varname>PCBOUNDS</varname>.</para>

		<para>Operations on a <varname>tpcpatch</varname> come at two granularities, because the points of a patch sit in a compressed payload that the bounding box layer cannot inspect. At <emphasis>patch level</emphasis> each instant's patch is kept or dropped as a whole, read from its 4-double <varname>PCBOUNDS</varname> header (xmin / xmax / ymin / ymax) and the instant's timestamp alone: this is the granularity of <link linkend="tpcpatch_atTpcbox"><varname>atTpcbox</varname> / <varname>minusTpcbox</varname></link>, of the bounding box operators and of the <varname>extent</varname> aggregate, it decompresses no payload, and it costs O(number of instants). <varname>PCBOUNDS</varname> being 2D, the Z dimension of a <varname>tpcbox</varname> argument is ignored there even when the schema has one.</para>

		<para>At <emphasis>point level</emphasis> every point of every instant is walked in C. <link linkend="tpcpatch_points"><varname>points</varname></link> emits one row per instant timestamp and point, while <link linkend="tpcpatch_atTpcboxFine"><varname>atTpcboxFine</varname> / <varname>minusTpcboxFine</varname></link>, <link linkend="tpcpatch_atGeometry"><varname>atGeometry</varname> / <varname>minusGeometry</varname></link> and <link linkend="tpcpatch_eIntersects"><varname>eIntersects(tpcpatch,geometry)</varname></link> apply their predicate to the actual coordinates and rebuild each surviving instant from the points that passed, dropping the instants that keep none. Prune at patch level first, with <varname>atTpcbox</varname> or a GiST / SP-GiST index scan, and refine the survivors when point-level fidelity is needed.</para>

		<sect2 xml:id="tpcpatch_inout">
			<title>Input and Output</title>
			<itemizedlist>
				<listitem xml:id="tpcpatch_asText">
					<indexterm significance="normal"><primary><varname>asText</varname></primary></indexterm>
					<para>Return the text representation of a tpcpatch, or of an array of them, and cast a tpcpatch to text</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
asText(tpcpatch) → text
asText(tpcpatch[]) → text[]
tpcpatch::text → text
</programlisting>
								<programlisting language="sql" xml:space="preserve" format="linespecific">
-- The pcpatch payload renders as hex-encoded WKB.
SELECT left(asText(tpcpatch(pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2)),
  '2001-01-01'::timestamptz)), 24);
-- EC0100000100000000000000
</programlisting>
				</listitem>
				<listitem xml:id="tpcpatch_asMFJSON">
					<indexterm significance="normal"><primary><varname>asMFJSON</varname></primary></indexterm>
					<para>For <varname>tpcpatch</varname>, the type tag is <varname>"MovingPCPatch"</varname> and each instant emits a small object <varname>{"pcid":N, "npoints":N, "bounds":[xmin,xmax,ymin,ymax]}</varname></para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
asMFJSON(tpcpatch,options int=0,flags int=0,maxdecimaldigits int=15) → text
</programlisting>
					<para>The compressed point payload is intentionally not in the JSON, use <varname>asBinary</varname> / <varname>asHexWKB</varname> for round-trip.</para>
								<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT asMFJSON(tpcpatch(pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2)),
  '2001-01-01'::timestamptz));
/* {"type":"MovingPCPatch","values":[{"pcid":1,"npoints":2,"bounds":[1,2,1,2]}],
   "datetimes":["2001-01-01T00:00:00"],"interpolation":"None"} */
</programlisting>
				</listitem>
			</itemizedlist>
			<para>As for a <varname>tpcpoint</varname>, <varname>asText</varname> takes no <varname>maxdecimaldigits</varname> argument and there is no <varname>tpcpatchFromText</varname>. MF-JSON is output-only, its per-instant summary dropping the per-point payload; for a lossless round trip use <varname>asBinary</varname> / <varname>tpcpatchFromBinary</varname> or <varname>asHexWKB</varname> / <varname>tpcpatchFromHexWKB</varname>.</para>
		</sect2>

		<sect2 xml:id="tpcpatch_constructors">
			<title>Constructors</title>
			<itemizedlist>
				<listitem xml:id="tpcpatch_tpcpatch">
					<indexterm significance="normal"><primary><varname>tpcpatch</varname></primary></indexterm>
					<para>Construct a temporal pcpatch of instant subtype</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
tpcpatch(pcpatch,timestamptz) → tpcpatch
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT tempSubtype(tpcpatch(pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2)),
  '2001-01-01'::timestamptz));
-- Instant
</programlisting>
				</listitem>

				<listitem xml:id="tpcpatch_tpcpatchSeq">
					<indexterm significance="normal"><primary><varname>tpcpatchSeq</varname></primary></indexterm>
					<para>Construct a temporal pcpatch of sequence subtype</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
tpcpatchSeq(tpcpatch[]) → tpcpatch
tpcpatchSeq(tpcpatch[],text) → tpcpatch
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT numInstants(tpcpatchSeq(ARRAY[
  tpcpatch(pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2)), '2001-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)),
  '2001-01-02'::timestamptz)])), interp(tpcpatchSeq(ARRAY[
  tpcpatch(pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2)), '2001-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)),
  '2001-01-02'::timestamptz)]));
-- 2 | Step
</programlisting>
				</listitem>

				<listitem xml:id="tpcpatch_tpcpatchSeqSet">
					<indexterm significance="normal"><primary><varname>tpcpatchSeqSet</varname></primary></indexterm>
					<para>Construct a temporal pcpatch of sequence-set subtype</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
tpcpatchSeqSet(tpcpatch[]) → tpcpatch
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT numSequences(tpcpatchSeqSet(ARRAY[tpcpatchSeq(ARRAY[ tpcpatch(pcpatch(pcpoint(1, 1,
  1, 1), pcpoint(1, 2, 2, 2)), '2001-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)),
  '2001-01-02'::timestamptz)])]));
-- 1
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcpatch_conversions">
			<title>Conversions</title>
			<itemizedlist>
				<listitem xml:id="tpcpatch_tgeometry">
					<indexterm significance="normal"><primary><varname>tgeometry</varname></primary></indexterm>
					<para>Convert a temporal patch into the temporal geometry of the multipoints its patches occupy</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
tgeometry(tpcpatch) &#8594; tgeometry
</programlisting>
					<para>Each instant's patch becomes the multipoint of the positions its points occupy, read through the schema its <varname>pcid</varname> names</para>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT asText(tpcpatch(pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2)),
  '2001-01-01'::timestamptz)::tgeometry);
-- MULTIPOINT Z ((1 1 1),(2 2 2))@2001-01-01
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcpatch_accessors">
			<title>Accessors</title>
			<para>All the generic temporal accessors apply, plus the <varname>tpcpoint</varname>-style <varname>pcid</varname> and <varname>SRID</varname>. In addition:</para>
			<itemizedlist>
				<listitem xml:id="tpcpatch_startNumPoints">
					<indexterm significance="normal"><primary><varname>startNumPoints</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>endNumPoints</varname></primary></indexterm>
					<para>Return the number of points in the patch carried by the first or last instant</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
startNumPoints(tpcpatch) → integer
endNumPoints(tpcpatch) → integer
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT startNumPoints(tpcpatch(pcpatch(pcpoint(1, 10.0, 20.0, 30.0)),
  '2024-02-01'::timestamptz));
-- 1
</programlisting>
				</listitem>

				<listitem xml:id="tpcpatch_numPoints">
					<indexterm significance="normal"><primary><varname>numPoints</varname></primary></indexterm>
					<para>Return the total number of points across every instant's patch</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
numPoints(tpcpatch) → bigint
</programlisting>
					<para>Read directly from each patch's header, no decompression.</para>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT numPoints(tpcpatchSeq(ARRAY[ tpcpatch(pcpatch(pcpoint(1, 1.0, 1.0, 1.0),
  pcpoint(1, 2.0, 2.0, 2.0)), '2024-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 3.0, 3.0, 3.0)), '2024-01-02'::timestamptz)]));
-- 3
</programlisting>
				</listitem>

				<listitem xml:id="tpcpatch_points">
					<indexterm significance="normal"><primary><varname>points</varname></primary></indexterm>
					<para>Set-returning function: emits one row per (instant timestamp, point) by walking each instant's patch and decomposing it through pgPointCloud's in-memory C API</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
points(tpcpatch) → setof (t timestamptz,point pcpoint)
</programlisting>
					<para>Useful for joining a tpcpatch column against per-point predicates that the bbox-level operators can't express. Cost is O(total points), one per-instant decompression plus one row emission per point.</para>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT t,
  point FROM points(tpcpatch(pcpatch(pcpoint(1, 1.0, 1.0, 1.0),
  pcpoint(1, 2.0, 2.0, 2.0)), '2024-01-01'::timestamptz));
-- ('2024-01-01',
-- '01:0000000000000000000000F03F0000000000000000F03F00000000000000F03F'::pcpoint)
-- ('2024-01-01',
-- '01:0000000000000000000000004000000000000000400000000000000040'::pcpoint)
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcpatch_transformations">
			<title>Transformations</title>
			<itemizedlist>
				<listitem xml:id="tpcpatch_tpcpatchInst">
					<indexterm significance="normal"><primary><varname>tpcpatchInst</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>tpcpatchSeq</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>tpcpatchSeqSet</varname></primary></indexterm>
					<para>Transform a tpcpatch to another subtype</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
tpcpatchInst(tpcpatch) → tpcpatch
tpcpatchSeq(tpcpatch,interp text='step') → tpcpatch
tpcpatchSeqSet(tpcpatch) → tpcpatch
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT tempSubtype(tpcpatchSeq(tpcpatch(pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2)),
  '2001-01-01'::timestamptz)));
-- Sequence
</programlisting>
				</listitem>
				<listitem xml:id="tpcpatch_setInterp">
					<indexterm significance="normal"><primary><varname>setInterp</varname></primary></indexterm>
					<para>Transform a tpcpatch to another interpolation</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
setInterp(tpcpatch,interp) → tpcpatch
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT interp(setInterp(tpcpatchSeq(ARRAY[ tpcpatch(pcpatch(pcpoint(1, 1, 1, 1),
  pcpoint(1, 2, 2, 2)), '2001-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)), '2001-01-02'::timestamptz)],
  'discrete'), 'step'));
-- Step
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcpatch_modifications">
			<title>Modifications</title>
			<itemizedlist>
				<listitem xml:id="tpcpatch_insert">
					<indexterm significance="normal"><primary><varname>insert</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>update</varname></primary></indexterm>
					<para>Insert a tpcpatch into another one, or update another one with it</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
insert(tpcpatch,tpcpatch,connect boolean=true) → tpcpatch
update(tpcpatch,tpcpatch,connect boolean=true) → tpcpatch
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT numInstants(insert(tpcpatchSeq(ARRAY[
  tpcpatch(pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2)), '2001-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)),
  '2001-01-02'::timestamptz)]), tpcpatchSeq(ARRAY[
  tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)), '2001-01-02'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 5, 5, 5)), '2001-01-03'::timestamptz)])));
-- 3
</programlisting>
				</listitem>

				<listitem xml:id="tpcpatch_deleteTime">
					<indexterm significance="normal"><primary><varname>deleteTime</varname></primary></indexterm>
					<para>Delete a time selector (timestamp, set, period, or spanset) from a tpcpatch</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
deleteTime(tpcpatch,time,connect boolean=true) → tpcpatch
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
-- Deleting the middle instant splits the sequence into {[2001-01-01, 2001-01-02),
-- (2001-01-02, 2001-01-03]}; each half stores its bound at the deleted timestamp, giving
-- four instants.
SELECT numInstants(deleteTime(tpcpatchSeq(ARRAY[ tpcpatch(pcpatch(pcpoint(1, 1, 1, 1),
  pcpoint(1, 2, 2, 2)), '2001-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)), '2001-01-02'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 5, 5, 5)), '2001-01-03'::timestamptz)]),
  timestamptz '2001-01-02'));
-- 4
</programlisting>
				</listitem>

				<listitem xml:id="tpcpatch_appendInstant">
					<indexterm significance="normal"><primary><varname>appendInstant</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>appendSequence</varname></primary></indexterm>
					<para>Append an instant or a sequence to a tpcpatch</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
appendInstant(tpcpatch,tpcpatch) → tpcpatch
appendSequence(tpcpatch,tpcpatch) → tpcpatch
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT numInstants(appendInstant(tpcpatchSeq(ARRAY[ tpcpatch(pcpatch(pcpoint(1, 1, 1, 1),
  pcpoint(1, 2, 2, 2)), '2001-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)),
  '2001-01-02'::timestamptz)]),
  tpcpatch(pcpatch(pcpoint(1, 5, 5, 5)), '2001-01-03'::timestamptz)));
-- 3
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcpatch_restrictions">
			<title>Restrictions</title>
			<itemizedlist>
				<listitem xml:id="tpcpatch_atValue">
					<indexterm significance="normal"><primary><varname>atValue</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>minusValue</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>atValues</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>minusValues</varname></primary></indexterm>
					<para>Restrict a tpcpatch to (the complement of) a pcpatch value or a set of pcpatch values</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
atValue(tpcpatch,pcpatch) → tpcpatch
minusValue(tpcpatch,pcpatch) → tpcpatch
atValues(tpcpatch,pcpatchset) → tpcpatch
minusValues(tpcpatch,pcpatchset) → tpcpatch
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
-- The matching value is held up to the next instant; the closing bound is stored as a
-- second instant.
SELECT numInstants(atValue(tpcpatchSeq(ARRAY[ tpcpatch(pcpatch(pcpoint(1, 1, 1, 1),
  pcpoint(1, 2, 2, 2)), '2001-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)),
  '2001-01-02'::timestamptz)]), pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2))));
-- 2
SELECT numInstants(minusValue(tpcpatchSeq(ARRAY[ tpcpatch(pcpatch(pcpoint(1, 1, 1, 1),
  pcpoint(1, 2, 2, 2)), '2001-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)),
  '2001-01-02'::timestamptz)]), pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2))));
-- 1
</programlisting>
				</listitem>

				<listitem xml:id="tpcpatch_atTpcbox">
					<indexterm significance="normal"><primary><varname>atTpcbox</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>minusTpcbox</varname></primary></indexterm>
					<para>Restrict a tpcpatch to the instants whose patch passes a coarse PCBOUNDS overlap test against the tpcbox, or remove them</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
atTpcbox(tpcpatch,tpcbox,border_inc boolean=true) → tpcpatch
minusTpcbox(tpcpatch,tpcbox,border_inc boolean=true) → tpcpatch
</programlisting>
					<para>Granularity is patch-level: each surviving instant keeps its pcpatch payload verbatim, with no per-point decompression. Because pgPointCloud's <varname>PCBOUNDS</varname> is 2D, the Z dimension of the box is ignored at this granularity. Returns NULL when the tpcbox's pcid does not match.</para>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT numInstants(atTpcbox(tpcpatchSeq(ARRAY[ tpcpatch(pcpatch(pcpoint(1, 1, 1, 1),
  pcpoint(1, 2, 2, 2)), '2001-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)),
  '2001-01-02'::timestamptz)]),
  tpcbox_xt(0, 0, 2, 2, tstzspan '[2001-01-01, 2001-01-03]', 1)));
-- 1
-- The dropped patch stays present on the open interval before its instant,
-- so the complement keeps two instants.
SELECT numInstants(minusTpcbox(tpcpatchSeq(ARRAY[ tpcpatch(pcpatch(pcpoint(1, 1, 1, 1),
  pcpoint(1, 2, 2, 2)), '2001-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)),
  '2001-01-02'::timestamptz)]),
  tpcbox_xt(0, 0, 2, 2, tstzspan '[2001-01-01, 2001-01-03]', 1)));
-- 2
</programlisting>
				</listitem>
				<listitem xml:id="tpcpatch_atTpcboxFine">
					<indexterm significance="normal"><primary><varname>atTpcboxFine</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>minusTpcboxFine</varname></primary></indexterm>
					<para>Restrict a tpcpatch to the points of each instant that the tpcbox holds, or remove them</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
atTpcboxFine(tpcpatch,tpcbox,border_inc boolean=true) → tpcpatch
minusTpcboxFine(tpcpatch,tpcbox,border_inc boolean=true) → tpcpatch
</programlisting>
					<para>Each surviving instant carries a freshly built pcpatch holding only the points inside, or outside for the minus form, the tpcbox in two dimensions, and in three when the box has a Z dimension. Instants whose patch filters to zero points are dropped. Slower than the coarse variant because every patch is decompressed and rebuilt; use when you need point-level fidelity.</para>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT numInstants(atTpcboxFine(tpcpatchSeq(ARRAY[ tpcpatch(pcpatch(pcpoint(1, 1, 1, 1),
  pcpoint(1, 2, 2, 2)), '2001-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)),
  '2001-01-02'::timestamptz)]),
  tpcbox_xt(0, 0, 1, 1, tstzspan '[2001-01-01, 2001-01-03]', 1)));
-- 1
</programlisting>
				</listitem>
				<listitem xml:id="tpcpatch_atGeometry">
					<indexterm significance="normal"><primary><varname>atGeometry</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>minusGeometry</varname></primary></indexterm>
					<para>Restrict a tpcpatch to the points whose XY projection intersects (or does not intersect, for minus) a 2D geometry</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
atGeometry(tpcpatch,geometry) → tpcpatch
minusGeometry(tpcpatch,geometry) → tpcpatch
</programlisting>
					<para>Z is ignored. SRID compatibility between the patch schema and the geometry must be ensured by the caller.</para>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
-- The polygon boundary touches the point (3 3) of the patch at 2001-01-02, so that patch
-- survives both the at and the minus side.
SELECT numInstants(atGeometry(tpcpatchSeq(ARRAY[ tpcpatch(pcpatch(pcpoint(1, 1, 1, 1),
  pcpoint(1, 2, 2, 2)), '2001-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)),
  '2001-01-02'::timestamptz)]), geometry 'POLYGON((0 0, 0 3, 3 3, 3 0, 0 0))'));
-- 2
SELECT numInstants(minusGeometry(tpcpatchSeq(ARRAY[ tpcpatch(pcpatch(pcpoint(1, 1, 1, 1),
  pcpoint(1, 2, 2, 2)), '2001-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)),
  '2001-01-02'::timestamptz)]), geometry 'POLYGON((0 0, 0 3, 3 3, 3 0, 0 0))'));
-- 1
</programlisting>
				</listitem>

				<listitem xml:id="tpcpatch_beforeTimestamp">
					<indexterm significance="normal"><primary><varname>beforeTimestamp</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>afterTimestamp</varname></primary></indexterm>
					<para>Restrict a tpcpatch to the instants before or after a timestamp</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
beforeTimestamp(tpcpatch,timestamptz,strict boolean=true) → tpcpatch
afterTimestamp(tpcpatch,timestamptz,strict boolean=true) → tpcpatch
</programlisting>
					<para>The strict flag excludes the instant at the timestamp itself</para>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT timeSpan(beforeTimestamp(tpcpatchSeq(ARRAY[ tpcpatch(pcpatch(pcpoint(1, 1, 1, 1),
  pcpoint(1, 2, 2, 2)), '2001-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)), '2001-01-02'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 5, 5, 5)), '2001-01-03'::timestamptz)]),
  timestamptz '2001-01-02'));
-- [2001-01-01, 2001-01-02)
SELECT timeSpan(afterTimestamp(tpcpatchSeq(ARRAY[ tpcpatch(pcpatch(pcpoint(1, 1, 1, 1),
  pcpoint(1, 2, 2, 2)), '2001-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)), '2001-01-02'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 5, 5, 5)), '2001-01-03'::timestamptz)]),
  timestamptz '2001-01-02'));
-- (2001-01-02, 2001-01-03]
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcpatch_splitting">
			<title>Splitting Operations</title>
			<itemizedlist>
				<listitem xml:id="tpcpatch_unnest">
					<indexterm significance="normal"><primary><varname>unnest</varname></primary></indexterm>
					<para>Return the distinct values of a tpcpatch with the span set on which each of them is taken</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
unnest(tpcpatch) → {(value,time)}
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT (un).time FROM (SELECT unnest(tpcpatchSeq(ARRAY[ tpcpatch(pcpatch(pcpoint(1, 1, 1,
  1), pcpoint(1, 2, 2, 2)), '2024-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 5, 5, 5), pcpoint(1, 6, 6, 6)), '2024-01-02'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2)),
  '2024-01-03'::timestamptz)])) AS un) t;
-- {[2024-01-02, 2024-01-03)}
-- {[2024-01-01, 2024-01-02), [2024-01-03, 2024-01-03]}
</programlisting>
				</listitem>

				<listitem xml:id="tpcpatch_timeSplit">
					<indexterm significance="normal"><primary><varname>timeSplit</varname></primary></indexterm>
					<para>Split a tpcpatch into fragments, one per time bin</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
timeSplit(tpcpatch,bin_width interval,
  origin timestamptz='2000-01-03') → {(time,tpcpatch)}
</programlisting>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT (ts).time, numInstants((ts).temp) FROM (SELECT timeSplit(tpcpatchSeq(ARRAY[
  tpcpatch(pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2)), '2024-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 5, 5, 5), pcpoint(1, 6, 6, 6)), '2024-01-02'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2)),
  '2024-01-03'::timestamptz)]),
  interval '1 day', '2024-01-01'::timestamptz) AS ts) t;
-- 2024-01-01 | 2
-- 2024-01-02 | 2
-- 2024-01-03 | 1
</programlisting>
				</listitem>

				<listitem xml:id="tpcpatch_spans">
					<indexterm significance="normal"><primary><varname>spans</varname></primary></indexterm>
					<para>Return the array of time spans of a tpcpatch's segments</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
spans(tpcpatch) → tstzspan[]
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT spans(tpcpatchSeq(ARRAY[tpcpatch(pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2)),
  '2024-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 5, 5, 5), pcpoint(1, 6, 6, 6)), '2024-01-02'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2)),
  '2024-01-03'::timestamptz)]));
-- {"[2024-01-01, 2024-01-02]","[2024-01-02, 2024-01-03]"}
</programlisting>
				</listitem>

				<listitem xml:id="tpcpatch_splitNSpans">
					<indexterm significance="normal"><primary><varname>splitNSpans</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>splitEachNSpans</varname></primary></indexterm>
					<para>Return the time spans of a tpcpatch split into a given number of bins, or with a given number of segments per bin</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
splitNSpans(tpcpatch,integer) → tstzspan[]
splitEachNSpans(tpcpatch,integer) → tstzspan[]
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT splitNSpans(tpcpatchSeq(ARRAY[ tpcpatch(pcpatch(pcpoint(1, 1, 1, 1),
  pcpoint(1, 2, 2, 2)), '2024-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 5, 5, 5), pcpoint(1, 6, 6, 6)), '2024-01-02'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2)),
  '2024-01-03'::timestamptz)]), 2);
-- {"[2024-01-01, 2024-01-02]","[2024-01-02, 2024-01-03]"}
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcpatch_bbox_ops">
			<title>Bounding Box Operations</title>
			<para>The same bbox operator surface as <xref linkend="tpcpoint_bbox_ops" /> applies to <varname>tpcpatch</varname>. The predicate evaluates against the value's <varname>tpcbox</varname>, computed in O(1) per instant from the patch's embedded <varname>PCBOUNDS</varname>.</para>
			<itemizedlist>
				<listitem xml:id="tpcpatch_topo">
					<indexterm significance="normal"><primary><varname>&amp;&amp;, @&gt;, &lt;@, ~=, -|-</varname></primary></indexterm>
					<para>Topological operators</para>
					<para>See <xref linkend="tpcbox_topo" />.</para>
				</listitem>
				<listitem xml:id="tpcpatch_pos">
					<indexterm significance="normal"><primary><varname>&lt;&lt;, &gt;&gt;, &lt;&lt;|, |&gt;&gt;, &lt;&lt;/, /&gt;&gt;, &lt;&lt;#, #&gt;&gt;</varname></primary></indexterm>
					<para>Position operators</para>
					<para>See <xref linkend="tpcbox_pos" />.</para>
				</listitem>
			</itemizedlist>
		</sect2>
		<sect2 xml:id="tpcpatch_distance_ops">
				<title>Distance Operations</title>
				<itemizedlist>
				<listitem xml:id="tpcpatch_nearestApproachDistance">
					<indexterm significance="normal"><primary><varname>|=|</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>nearestApproachDistance</varname></primary></indexterm>
					<para>Return the smallest distance ever</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
nearestApproachDistance(tpcpatch,{tpcbox,tpcpatch}) → float
</programlisting>
				</listitem>
				</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcpatch_spatialrels">
			<title>Ever and Always Relationships</title>
			<itemizedlist>
				<listitem xml:id="tpcpatch_eIntersects">
					<indexterm significance="normal"><primary><varname>eIntersects</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>aIntersects</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>eContains</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>aContains</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>eCovers</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>aCovers</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>eDisjoint</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>aDisjoint</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>eTouches</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>aTouches</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>eDwithin</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>aDwithin</varname></primary></indexterm>
					<para>Return true iff at least one point in any of the tpcpatch's instants intersects the geometry (XY only)</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
eIntersects(tpcpatch,geometry) → boolean
</programlisting>
					<para>The ever and always relationships cover the same matrix: <varname>eContains</varname>, <varname>eCovers</varname>, <varname>eDisjoint</varname>, <varname>eIntersects</varname>, <varname>eTouches</varname> and <varname>eDwithin</varname>, each in the three argument orders, and their always twins. <varname>eIntersects(tpcpatch,geometry)</varname> is the one the type answers natively, walking the points of each instant and stopping at the first hit; the rest convert the patch to the temporal geometry of its multipoints.</para>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT eIntersects(tpcpatchSeq(ARRAY[ tpcpatch(pcpatch(pcpoint(1, 1, 1, 1),
  pcpoint(1, 2, 2, 2)), '2001-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)),
  '2001-01-02'::timestamptz)]), geometry 'POINT(1 1)');
-- t
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcpatch_spatiotemp_rels">
			<title>Spatiotemporal Relationships</title>
			<para>The <emphasis>temporal</emphasis> relationships compute the topological or distance relationship at each instant and result in a <varname>tbool</varname>. A patch reaches the geometry engine through the <varname>tgeometry</varname> conversion above, so one point of a patch inside the argument is enough for the patch to intersect it. A tpcpatch carries the Z its schema declares, and <varname>tContains</varname>, <varname>tCovers</varname> and <varname>tTouches</varname> are planar relationships that refuse a Z dimension, so the type answers the three that a Z-carrying value can.</para>
			<itemizedlist>
				<listitem xml:id="tpcpatch_tDisjoint">
					<indexterm significance="normal"><primary><varname>tContains</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>tCovers</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>tDisjoint</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>tDwithin</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>tIntersects</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>tTouches</varname></primary></indexterm>
					<para>Temporal spatial relationships</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
tDisjoint({geometry,tpcpatch},{geometry,tpcpatch}) &#8594; tbool
tDwithin({geometry,tpcpatch},{geometry,tpcpatch},float) &#8594; tbool
tIntersects({geometry,tpcpatch},{geometry,tpcpatch}) &#8594; tbool
tContains({geometry,tpcpatch},{geometry,tpcpatch}) &#8594; tbool
tCovers({geometry,tpcpatch},{geometry,tpcpatch}) &#8594; tbool
tTouches({geometry,tpcpatch},{geometry,tpcpatch}) &#8594; tbool
</programlisting>
					<para>A patch reaches the geometry engine as the multipoint of the positions its points occupy, so it declares the matrix that temporal geometry declares: every predicate in every direction. The planar ones answer for a schema whose dimensions do not include Z, and a schema carrying Z meets the refusal <varname>The tgeometry cannot have Z dimension</varname>.</para>
					<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT tIntersects(tpcpatch(pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 9, 9, 9)),
  '2001-01-01'::timestamptz), geometry 'Polygon((0 0,0 2,2 2,2 0,0 0))');
-- t@2001-01-01
SELECT tDisjoint(tpcpatch(pcpatch(pcpoint(1, 9, 9, 9)), '2001-01-02'::timestamptz),
  geometry 'Polygon((0 0,0 2,2 2,2 0,0 0))');
-- t@2001-01-02
SELECT tDwithin(tpcpatch(pcpatch(pcpoint(1, 1, 1, 1)), '2001-01-01'::timestamptz),
  geometry 'Point(1 5)', 2.0);
-- f@2001-01-01
SELECT tIntersects(tpcpatchSeq(ARRAY[ tpcpatch(pcpatch(pcpoint(1, 1, 1, 1)),
  '2001-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 9, 9, 9)), '2001-01-02'::timestamptz)]),
  geometry 'Polygon((0 0,0 2,2 2,2 0,0 0))');
-- [t@2001-01-01, f@2001-01-02]
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

		<sect2 xml:id="tpcpatch_comparisons">
			<title>Comparisons</title>
			<itemizedlist>
				<listitem xml:id="tpcpatch_ever_eq">
					<indexterm significance="normal"><primary><varname>?=</varname></primary></indexterm>
					<indexterm significance="normal"><primary><varname>%=</varname></primary></indexterm>
					<para>Ever and always comparisons</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
{pcpatch,tpcpatch} {?=, %=, ?&lt;&gt;, %&lt;&gt;} {pcpatch,tpcpatch} → boolean
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2)) ?= tpcpatchSeq(ARRAY[
  tpcpatch(pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2)), '2001-01-01'::timestamptz),
  tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)),
  '2001-01-02'::timestamptz)]);
-- t
SELECT tpcpatchSeq(ARRAY[tpcpatch(pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2)),
  '2001-01-01'::timestamptz), tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)),
  '2001-01-02'::timestamptz)]) %= pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2));
-- f
</programlisting>
				</listitem>

				<listitem xml:id="tpcpatch_temporal_eq">
					<indexterm significance="normal"><primary><varname>#=</varname></primary></indexterm>
					<para>Temporal comparisons</para>
					<programlisting role="syntax" xml:space="preserve" format="linespecific">
{pcpatch,tpcpatch} {#=, #&lt;&gt;} {pcpatch,tpcpatch} → tbool
</programlisting>
									<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT tpcpatchSeq(ARRAY[tpcpatch(pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2)),
  '2001-01-01'::timestamptz), tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)),
  '2001-01-02'::timestamptz)]) #= pcpatch(pcpoint(1, 1, 1, 1), pcpoint(1, 2, 2, 2));
-- [t@2001-01-01, f@2001-01-02]
</programlisting>
				</listitem>
			</itemizedlist>
		</sect2>

	</sect1>

	<sect1 xml:id="tpointcloud_indexes">
		<title>Indexing</title>

		<para>B-tree (<varname>tpcpoint_btree_ops</varname>, <varname>tpcpatch_btree_ops</varname>, <varname>tpcbox_btree_ops</varname>) and hash (<varname>tpcpoint_hash_ops</varname>, <varname>tpcpatch_hash_ops</varname>) operator classes support equality and ordering predicates. The R-tree GiST and quadtree / kd-tree SP-GiST operator classes accelerate the bbox-based predicates (<varname>&amp;&amp;</varname>, <varname>@&gt;</varname>, <varname>&lt;@</varname>, <varname>~=</varname>, <varname>-|-</varname>) on all three of <varname>tpcbox</varname>, <varname>tpcpoint</varname>, and <varname>tpcpatch</varname>:</para>

		<programlisting language="sql" xml:space="preserve" format="linespecific">
-- GiST (R-tree)
CREATE INDEX ON my_table USING gist(my_tpcpoint_column);
CREATE INDEX ON my_table USING gist(my_tpcpatch_column);
-- SP-GiST quadtree (default) or kd-tree
CREATE INDEX ON my_table USING spgist(my_tpcpoint_column);
CREATE INDEX ON my_table USING spgist(my_tpcpoint_column tpcpoint_kdtree_ops);
</programlisting>

		<para>SP-GiST opclasses store an <varname>stbox</varname> internally (the <varname>pcid</varname> filter is recovered by the operator's recheck), so a query against an index on a column whose values mix multiple <varname>pcid</varname>s will return a slightly larger set of candidate rows for recheck, a non-issue when each table holds values of a single schema, which is the common case.</para>
	</sect1>

	<sect1 xml:id="tpointcloud_aggregations">
		<title>Aggregations</title>
		<itemizedlist>
			<listitem xml:id="tpcpoint_extent">
				<indexterm significance="normal"><primary><varname>extent</varname></primary></indexterm>
				<para>Bounding-box aggregate over a column of tpcpoint, tpcpatch, or tpcbox values</para>
				<programlisting role="syntax" xml:space="preserve" format="linespecific">
extent({tpcpoint,tpcpatch,tpcbox}) → tpcbox
</programlisting>
				<para>Returns the smallest tpcbox containing every input. Aggregating across distinct pcids raises an error, the bbox would be uninterpretable since dimensions are pcid-relative.</para>
							<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT extent(v) FROM (VALUES (tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz)),
  (tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz))) t(v);
-- TPCBOX(ZT(((1,1,1),(2,2,2)),[2001-01-01, 2001-01-02]), 1)
</programlisting>
			</listitem>

			<listitem xml:id="tpcpoint_tCount">
				<indexterm significance="normal"><primary><varname>tCount</varname></primary></indexterm>
				<indexterm significance="normal"><primary><varname>wCount</varname></primary></indexterm>
				<para>Temporal count and window count of overlapping rows at each timestamp</para>
				<programlisting role="syntax" xml:space="preserve" format="linespecific">
tCount({tpcpoint,tpcpatch}) → tint
wCount({tpcpoint,tpcpatch},interval) → tint
</programlisting>
				<para>Result is a tint. All input rows must share the same temporal subtype (instant / sequence / sequence set).</para>
							<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT tCount(v) FROM (VALUES (tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz)),
  (tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz))) t(v);
-- {1@2001-01-01, 1@2001-01-02}
SELECT wCount(v, interval '1 day') FROM (VALUES
  (tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz)),
  (tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz))) t(v);
-- {[1@2001-01-01, 2@2001-01-02], (1@2001-01-02, 1@2001-01-03]}
</programlisting>
			</listitem>

			<listitem xml:id="tpcpatch_tnpoints">
				<indexterm significance="normal"><primary><varname>tnpoints</varname></primary></indexterm>
				<para>Per-instant pcpatch point count, summed across rows</para>
				<programlisting role="syntax" xml:space="preserve" format="linespecific">
tnpoints(tpcpatch) → tint
</programlisting>
				<para>Distinct from <varname>tCount(tpcpatch)</varname>, which counts the number of patches (always 1 per instant). <varname>tnpoints</varname> reads as "how many points were in the cloud at time t", the natural primitive for ingest-QC and density-over-time dashboards.</para>
							<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT tnpoints(v) FROM (VALUES (tpcpatch(pcpatch(pcpoint(1, 1, 1, 1),
  pcpoint(1, 2, 2, 2)), '2001-01-01'::timestamptz)),
  (tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)),
  '2001-01-02'::timestamptz))) t(v);
-- {2@2001-01-01, 2@2001-01-02}
</programlisting>
			</listitem>

			<listitem xml:id="tpcpatch_tdensity">
				<indexterm significance="normal"><primary><varname>tdensity</varname></primary></indexterm>
				<para>Per-instant point density (<varname>numPoints / xy-bbox-area</varname>) summed across rows</para>
				<programlisting role="syntax" xml:space="preserve" format="linespecific">
tdensity(tpcpatch) → tfloat
</programlisting>
				<para>Each pcpatch carries the inline <varname>PCBOUNDS</varname> (xmin / xmax / ymin / ymax) so the bbox-area term is read directly, no recomputation. A 1-point or co-linear patch yields <varname>+Infinity</varname> for that instant; filter with <varname>isfinite()</varname> in downstream queries if needed.</para>
							<programlisting language="sql" xml:space="preserve" format="linespecific">
-- Each patch holds 2 points over a 1x1 xy-bbox.
SELECT tdensity(v) FROM (VALUES (tpcpatch(pcpatch(pcpoint(1, 1, 1, 1),
  pcpoint(1, 2, 2, 2)), '2001-01-01'::timestamptz)),
  (tpcpatch(pcpatch(pcpoint(1, 3, 3, 3), pcpoint(1, 4, 4, 4)),
  '2001-01-02'::timestamptz))) t(v);
-- {2@2001-01-01, 2@2001-01-02}
</programlisting>
			</listitem>

			<listitem xml:id="tpcpoint_mergeAgg">
				<indexterm significance="normal"><primary><varname>merge</varname></primary></indexterm>
				<indexterm significance="normal"><primary><varname>mergeAgg</varname></primary></indexterm>
				<para>Combine multiple temporal values into a single one with all input instants merged in temporal order</para>
				<programlisting role="syntax" xml:space="preserve" format="linespecific">
mergeAgg(tpcpoint) → tpcpoint
mergeAgg(tpcpatch) → tpcpatch
</programlisting>
				<para>All inputs must share the same pcid and subtype.</para>
							<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT numInstants(mergeAgg(v)) FROM (VALUES (tpcpoint(pcpoint(1, 1, 1, 1),
  '2001-01-01'::timestamptz)),
  (tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz))) t(v);
-- 2
</programlisting>
			</listitem>

			<listitem xml:id="tpcpoint_appendInstantAgg">
				<indexterm significance="normal"><primary><varname>appendInstant</varname></primary></indexterm>
				<indexterm significance="normal"><primary><varname>appendSequence</varname></primary></indexterm>
				<indexterm significance="normal"><primary><varname>appendInstantAgg</varname></primary></indexterm>
				<indexterm significance="normal"><primary><varname>appendSequenceAgg</varname></primary></indexterm>
				<para>Streaming construction aggregates: <varname>appendInstant</varname> assembles instants into a sequence; <varname>appendSequence</varname> assembles sequences into a sequence set</para>
				<programlisting role="syntax" xml:space="preserve" format="linespecific">
appendInstantAgg(tpcpoint[,interp,maxt]) → tpcpoint
appendInstantAgg(tpcpatch) → tpcpatch
appendSequenceAgg(tpcpoint) → tpcpoint
appendSequenceAgg(tpcpatch) → tpcpatch
</programlisting>
				<para>Useful for trajectory loaders that consume rows in time order.</para>
							<programlisting language="sql" xml:space="preserve" format="linespecific">
SELECT numInstants(appendInstant(v ORDER BY startTimestamp(v))) FROM (VALUES
  (tpcpoint(pcpoint(1, 1, 1, 1), '2001-01-01'::timestamptz)),
  (tpcpoint(pcpoint(1, 2, 2, 2), '2001-01-02'::timestamptz))) t(v);
-- 2
</programlisting>
			</listitem>
		</itemizedlist>
	</sect1>

</chapter>
