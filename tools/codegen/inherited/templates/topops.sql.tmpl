@LICENSE@

/**
 * @file
 * @brief Bounding box operators for {BRIEF}
 */

-- @IF box_extraction
/*****************************************************************************
 * Temporal {BASE} to stbox
 *****************************************************************************/

-- @IF scalar_stbox_cast
CREATE FUNCTION stbox({BASE})
  RETURNS stbox
  AS 'MODULE_PATHNAME', '{CBASE}_to_stbox'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION stbox({BASE}, timestamptz)
  RETURNS stbox
  AS 'MODULE_PATHNAME', '{CBASE}_timestamptz_to_stbox'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION stbox({BASE}, tstzspan)
  RETURNS stbox
  AS 'MODULE_PATHNAME', '{CBASE}_tstzspan_to_stbox'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;
-- @ENDIF

CREATE FUNCTION stbox({TEMP})
  RETURNS stbox
  AS 'MODULE_PATHNAME', 'Tspatial_to_stbox'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

-- @IF scalar_stbox_cast
CREATE CAST ({BASE} AS stbox) WITH FUNCTION stbox({BASE});
-- @ENDIF
CREATE CAST ({TEMP} AS stbox) WITH FUNCTION stbox({TEMP});

/*****************************************************************************/

-- @IF stboxes
CREATE FUNCTION stboxes({TEMP})
  RETURNS stbox[]
  AS 'MODULE_PATHNAME', 'Tgeo_stboxes'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION splitNStboxes({TEMP}, integer)
  RETURNS stbox[]
  AS 'MODULE_PATHNAME', 'Tgeo_split_n_stboxes'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION splitEachNStboxes({TEMP}, integer)
  RETURNS stbox[]
  AS 'MODULE_PATHNAME', 'Tgeo_split_each_n_stboxes'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

/*****************************************************************************/

-- @ENDIF
CREATE FUNCTION expandSpace({TEMP}, float)
  RETURNS stbox
  AS 'SELECT @extschema@.expandSpace($1::stbox, $2)'
  LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE;

-- @ENDIF
/*****************************************************************************
 * Contains
 *****************************************************************************/

CREATE FUNCTION contains(tstzspan, {TEMP})
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Contains_tstzspan_temporal'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;
CREATE FUNCTION contains({TEMP}, tstzspan)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Contains_temporal_tstzspan'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE OPERATOR @> (
  PROCEDURE = contains,
  LEFTARG = tstzspan, RIGHTARG = {TEMP},
  COMMUTATOR = <@,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);
CREATE OPERATOR @> (
  PROCEDURE = contains,
  LEFTARG = {TEMP}, RIGHTARG = tstzspan,
  COMMUTATOR = <@,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);

/*****************************************************************************/

CREATE FUNCTION contains(stbox, {TEMP})
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Contains_stbox_tspatial'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;
CREATE FUNCTION contains({TEMP}, stbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Contains_tspatial_stbox'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;
CREATE FUNCTION contains({TEMP}, {TEMP})
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Contains_tspatial_tspatial'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE OPERATOR @> (
  PROCEDURE = contains,
  LEFTARG = stbox, RIGHTARG = {TEMP},
  COMMUTATOR = <@,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);
CREATE OPERATOR @> (
  PROCEDURE = contains,
  LEFTARG = {TEMP}, RIGHTARG = stbox,
  COMMUTATOR = <@,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);
CREATE OPERATOR @> (
  PROCEDURE = contains,
  LEFTARG = {TEMP}, RIGHTARG = {TEMP},
  COMMUTATOR = <@,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);

/*****************************************************************************
 * Contained
 *****************************************************************************/

CREATE FUNCTION contained(tstzspan, {TEMP})
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Contained_tstzspan_temporal'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;
CREATE FUNCTION contained({TEMP}, tstzspan)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Contained_temporal_tstzspan'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE OPERATOR <@ (
  PROCEDURE = contained,
  LEFTARG = tstzspan, RIGHTARG = {TEMP},
  COMMUTATOR = @>,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);
CREATE OPERATOR <@ (
  PROCEDURE = contained,
  LEFTARG = {TEMP}, RIGHTARG = tstzspan,
  COMMUTATOR = @>,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);

/*****************************************************************************/

CREATE FUNCTION contained(stbox, {TEMP})
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Contained_stbox_tspatial'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;
CREATE FUNCTION contained({TEMP}, stbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Contained_tspatial_stbox'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;
CREATE FUNCTION contained({TEMP}, {TEMP})
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Contained_tspatial_tspatial'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE OPERATOR <@ (
  PROCEDURE = contained,
  LEFTARG = stbox, RIGHTARG = {TEMP},
  COMMUTATOR = @>,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);
CREATE OPERATOR <@ (
  PROCEDURE = contained,
  LEFTARG = {TEMP}, RIGHTARG = stbox,
  COMMUTATOR = @>,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);
CREATE OPERATOR <@ (
  PROCEDURE = contained,
  LEFTARG = {TEMP}, RIGHTARG = {TEMP},
  COMMUTATOR = @>,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);

/*****************************************************************************
 * Overlaps
 *****************************************************************************/

CREATE FUNCTION overlaps(tstzspan, {TEMP})
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overlaps_tstzspan_temporal'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;
CREATE FUNCTION overlaps({TEMP}, tstzspan)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overlaps_temporal_tstzspan'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE OPERATOR && (
  PROCEDURE = overlaps,
  LEFTARG = tstzspan, RIGHTARG = {TEMP},
  COMMUTATOR = &&,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);
CREATE OPERATOR && (
  PROCEDURE = overlaps,
  LEFTARG = {TEMP}, RIGHTARG = tstzspan,
  COMMUTATOR = &&,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);

/*****************************************************************************/

CREATE FUNCTION overlaps(stbox, {TEMP})
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overlaps_stbox_tspatial'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;
CREATE FUNCTION overlaps({TEMP}, stbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overlaps_tspatial_stbox'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;
CREATE FUNCTION overlaps({TEMP}, {TEMP})
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overlaps_tspatial_tspatial'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE OPERATOR && (
  PROCEDURE = overlaps,
  LEFTARG = stbox, RIGHTARG = {TEMP},
  COMMUTATOR = &&,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);
CREATE OPERATOR && (
  PROCEDURE = overlaps,
  LEFTARG = {TEMP}, RIGHTARG = stbox,
  COMMUTATOR = &&,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);
CREATE OPERATOR && (
  PROCEDURE = overlaps,
  LEFTARG = {TEMP}, RIGHTARG = {TEMP},
  COMMUTATOR = &&,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);

/*****************************************************************************
 * Same
 *****************************************************************************/

CREATE FUNCTION same(tstzspan, {TEMP})
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Same_tstzspan_temporal'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;
CREATE FUNCTION same({TEMP}, tstzspan)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Same_temporal_tstzspan'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE OPERATOR ~= (
  PROCEDURE = same,
  LEFTARG = tstzspan, RIGHTARG = {TEMP},
  COMMUTATOR = ~=,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);
CREATE OPERATOR ~= (
  PROCEDURE = same,
  LEFTARG = {TEMP}, RIGHTARG = tstzspan,
  COMMUTATOR = ~=,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);

/*****************************************************************************/

CREATE FUNCTION same(stbox, {TEMP})
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Same_stbox_tspatial'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;
CREATE FUNCTION same({TEMP}, stbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Same_tspatial_stbox'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;
CREATE FUNCTION same({TEMP}, {TEMP})
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Same_tspatial_tspatial'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE OPERATOR ~= (
  PROCEDURE = same,
  LEFTARG = stbox, RIGHTARG = {TEMP},
  COMMUTATOR = ~=,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);
CREATE OPERATOR ~= (
  PROCEDURE = same,
  LEFTARG = {TEMP}, RIGHTARG = stbox,
  COMMUTATOR = ~=,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);
CREATE OPERATOR ~= (
  PROCEDURE = same,
  LEFTARG = {TEMP}, RIGHTARG = {TEMP},
  COMMUTATOR = ~=,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);

/*****************************************************************************
 * Adjacent
 *****************************************************************************/

CREATE FUNCTION adjacent(tstzspan, {TEMP})
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Adjacent_tstzspan_temporal'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;
CREATE FUNCTION adjacent({TEMP}, tstzspan)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Adjacent_temporal_tstzspan'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE OPERATOR -|- (
  PROCEDURE = adjacent,
  LEFTARG = tstzspan, RIGHTARG = {TEMP},
  COMMUTATOR = -|-,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);
CREATE OPERATOR -|- (
  PROCEDURE = adjacent,
  LEFTARG = {TEMP}, RIGHTARG = tstzspan,
  COMMUTATOR = -|-,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);

/*****************************************************************************/

CREATE FUNCTION adjacent(stbox, {TEMP})
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Adjacent_stbox_tspatial'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;
CREATE FUNCTION adjacent({TEMP}, stbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Adjacent_tspatial_stbox'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;
CREATE FUNCTION adjacent({TEMP}, {TEMP})
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Adjacent_tspatial_tspatial'
  SUPPORT {SEL}_supportfn
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE OPERATOR -|- (
  PROCEDURE = adjacent,
  LEFTARG = stbox, RIGHTARG = {TEMP},
  COMMUTATOR = -|-,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);
CREATE OPERATOR -|- (
  PROCEDURE = adjacent,
  LEFTARG = {TEMP}, RIGHTARG = stbox,
  COMMUTATOR = -|-,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);
CREATE OPERATOR -|- (
  PROCEDURE = adjacent,
  LEFTARG = {TEMP}, RIGHTARG = {TEMP},
  COMMUTATOR = -|-,
  RESTRICT = {SEL}_sel, JOIN = {SEL}_joinsel
);

/*****************************************************************************/
