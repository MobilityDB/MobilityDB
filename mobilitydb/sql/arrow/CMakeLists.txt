SET(LOCAL_FILES
  600_arrow
  )

if(CBUFFER)
  list(APPEND LOCAL_FILES 601_arrow_cbuffer)
endif()
if(NPOINT)
  list(APPEND LOCAL_FILES 602_arrow_npoint)
endif()
if(POSE)
  list(APPEND LOCAL_FILES 603_arrow_pose)
endif()
if(RGEO)
  list(APPEND LOCAL_FILES 604_arrow_rgeo)
endif()
if(H3)
  list(APPEND LOCAL_FILES 605_arrow_h3)
endif()
if(QUADBIN)
  list(APPEND LOCAL_FILES 606_arrow_quadbin)
endif()
if(POINTCLOUD)
  list(APPEND LOCAL_FILES 607_arrow_pointcloud)
endif()
if(JSON)
  list(APPEND LOCAL_FILES 608_arrow_json)
endif()

foreach (f ${LOCAL_FILES})
  process_file(${f} ${CMAKE_CURRENT_SOURCE_DIR} ${CMAKE_CURRENT_BINARY_DIR})
  configure_file(${CMAKE_CURRENT_BINARY_DIR}/${f}.sql.in ${CMAKE_BINARY_DIR}/MobilityDB/sql/${f}.sql @ONLY)
  list(APPEND PACKAGE_SQL_FILES  ${CMAKE_BINARY_DIR}/MobilityDB/sql/${f}.sql)
endforeach()

set(PROJECT_SQL_FILES ${PROJECT_SQL_FILES} ${PACKAGE_SQL_FILES} PARENT_SCOPE)
