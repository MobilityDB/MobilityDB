/*****************************************************************************
 *
 * This MobilityDB code is provided under The PostgreSQL License.
 * Copyright (c) 2016-2026, Université libre de Bruxelles and MobilityDB
 * contributors
 *
 *****************************************************************************/

/**
 * @file
 * @brief Portable bare-name aliases for MobilityDB operators.
 *
 * GENERATED by tools/portable_aliases/generate.py -- DO NOT EDIT.
 * Each alias reuses the operator's own backing C symbol; its
 * behaviour is identical to the operator (RFC: doc/rfc/sql-portability).
 */

CREATE FUNCTION left(tpcbox, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Left_tpcbox_tpcbox'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overleft(tpcbox, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overleft_tpcbox_tpcbox'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION right(tpcbox, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Right_tpcbox_tpcbox'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overright(tpcbox, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overright_tpcbox_tpcbox'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION below(tpcbox, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Below_tpcbox_tpcbox'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overbelow(tpcbox, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overbelow_tpcbox_tpcbox'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION above(tpcbox, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Above_tpcbox_tpcbox'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overabove(tpcbox, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overabove_tpcbox_tpcbox'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION front(tpcbox, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Front_tpcbox_tpcbox'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overfront(tpcbox, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overfront_tpcbox_tpcbox'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION back(tpcbox, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Back_tpcbox_tpcbox'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overback(tpcbox, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overback_tpcbox_tpcbox'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION before(tpcbox, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Before_tpcbox_tpcbox'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overbefore(tpcbox, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overbefore_tpcbox_tpcbox'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION after(tpcbox, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'After_tpcbox_tpcbox'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overafter(tpcbox, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overafter_tpcbox_tpcbox'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION left(tpcbox, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Left_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION left(tpcpoint, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Left_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION left(tpcpoint, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Left_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION left(tpcbox, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Left_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION left(tpcpatch, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Left_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION left(tpcpatch, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Left_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overleft(tpcbox, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overleft_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overleft(tpcpoint, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overleft_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overleft(tpcpoint, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overleft_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overleft(tpcbox, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overleft_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overleft(tpcpatch, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overleft_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overleft(tpcpatch, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overleft_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION right(tpcbox, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Right_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION right(tpcpoint, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Right_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION right(tpcpoint, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Right_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION right(tpcbox, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Right_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION right(tpcpatch, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Right_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION right(tpcpatch, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Right_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overright(tpcbox, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overright_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overright(tpcpoint, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overright_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overright(tpcpoint, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overright_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overright(tpcbox, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overright_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overright(tpcpatch, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overright_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overright(tpcpatch, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overright_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION below(tpcbox, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Below_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION below(tpcpoint, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Below_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION below(tpcpoint, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Below_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION below(tpcbox, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Below_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION below(tpcpatch, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Below_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION below(tpcpatch, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Below_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overbelow(tpcbox, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overbelow_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overbelow(tpcpoint, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overbelow_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overbelow(tpcpoint, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overbelow_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overbelow(tpcbox, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overbelow_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overbelow(tpcpatch, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overbelow_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overbelow(tpcpatch, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overbelow_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION above(tpcbox, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Above_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION above(tpcpoint, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Above_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION above(tpcpoint, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Above_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION above(tpcbox, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Above_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION above(tpcpatch, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Above_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION above(tpcpatch, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Above_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overabove(tpcbox, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overabove_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overabove(tpcpoint, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overabove_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overabove(tpcpoint, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overabove_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overabove(tpcbox, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overabove_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overabove(tpcpatch, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overabove_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overabove(tpcpatch, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overabove_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION front(tpcbox, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Front_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION front(tpcpoint, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Front_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION front(tpcpoint, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Front_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION front(tpcbox, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Front_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION front(tpcpatch, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Front_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION front(tpcpatch, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Front_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overfront(tpcbox, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overfront_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overfront(tpcpoint, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overfront_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overfront(tpcpoint, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overfront_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overfront(tpcbox, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overfront_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overfront(tpcpatch, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overfront_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overfront(tpcpatch, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overfront_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION back(tpcbox, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Back_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION back(tpcpoint, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Back_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION back(tpcpoint, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Back_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION back(tpcbox, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Back_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION back(tpcpatch, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Back_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION back(tpcpatch, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Back_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overback(tpcbox, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overback_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overback(tpcpoint, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overback_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overback(tpcpoint, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overback_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overback(tpcbox, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overback_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overback(tpcpatch, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overback_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overback(tpcpatch, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overback_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION before(tstzspan, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Before_tstzspan_temporal' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION before(tpcpoint, tstzspan)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Before_temporal_tstzspan' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION before(tpcbox, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Before_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION before(tpcpoint, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Before_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION before(tpcpoint, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Before_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION before(tstzspan, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Before_tstzspan_temporal' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION before(tpcpatch, tstzspan)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Before_temporal_tstzspan' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION before(tpcbox, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Before_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION before(tpcpatch, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Before_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION before(tpcpatch, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Before_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overbefore(tstzspan, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overbefore_tstzspan_temporal' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overbefore(tpcpoint, tstzspan)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overbefore_temporal_tstzspan' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overbefore(tpcbox, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overbefore_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overbefore(tpcpoint, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overbefore_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overbefore(tpcpoint, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overbefore_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overbefore(tstzspan, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overbefore_tstzspan_temporal' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overbefore(tpcpatch, tstzspan)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overbefore_temporal_tstzspan' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overbefore(tpcbox, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overbefore_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overbefore(tpcpatch, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overbefore_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overbefore(tpcpatch, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overbefore_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION after(tstzspan, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'After_tstzspan_temporal' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION after(tpcpoint, tstzspan)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'After_temporal_tstzspan' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION after(tpcbox, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'After_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION after(tpcpoint, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'After_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION after(tpcpoint, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'After_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION after(tstzspan, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'After_tstzspan_temporal' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION after(tpcpatch, tstzspan)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'After_temporal_tstzspan' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION after(tpcbox, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'After_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION after(tpcpatch, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'After_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION after(tpcpatch, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'After_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overafter(tstzspan, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overafter_tstzspan_temporal' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overafter(tpcpoint, tstzspan)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overafter_temporal_tstzspan' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overafter(tpcbox, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overafter_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overafter(tpcpoint, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overafter_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overafter(tpcpoint, tpcpoint)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overafter_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overafter(tstzspan, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overafter_tstzspan_temporal' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overafter(tpcpatch, tstzspan)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overafter_temporal_tstzspan' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overafter(tpcbox, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overafter_tpcbox_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overafter(tpcpatch, tpcbox)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overafter_tpointcloud_tpcbox' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION overafter(tpcpatch, tpcpatch)
  RETURNS boolean
  AS 'MODULE_PATHNAME', 'Overafter_tpointcloud_tpointcloud' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

