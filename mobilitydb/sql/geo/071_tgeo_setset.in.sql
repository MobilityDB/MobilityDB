/*****************************************************************************
 *
 * This MobilityDB code is provided under The PostgreSQL License.
 * Copyright (c) 2016-2026, Universite libre de Bruxelles and MobilityDB
 * contributors
 *
 *****************************************************************************/

/**
 * @file
 * @brief Set-set spatial-join functions for temporal geometries.
 *
 * GENERATED by tools/setset_join/generate.py -- DO NOT EDIT.
 * Each function is a thin wrapper over a type-generic C kernel; the surface
 * mirrors the type coverage of the scalar spatial relationships.
 */

CREATE FUNCTION minDistance(tgeometry[], tgeometry[])
  RETURNS float
  AS 'MODULE_PATHNAME', 'Tgeoarr_tgeoarr_mindist'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION minDistance(tgeography[], tgeography[])
  RETURNS float
  AS 'MODULE_PATHNAME', 'Tgeoarr_tgeoarr_mindist'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION minDistance(tgeometry, geometry)
  RETURNS float
  AS 'MODULE_PATHNAME', 'NAD_tgeo_geo'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;
CREATE FUNCTION minDistance(geometry, tgeometry)
  RETURNS float
  AS 'MODULE_PATHNAME', 'NAD_geo_tgeo'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION minDistance(tgeography, geography)
  RETURNS float
  AS 'MODULE_PATHNAME', 'NAD_tgeo_geo'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;
CREATE FUNCTION minDistance(geography, tgeography)
  RETURNS float
  AS 'MODULE_PATHNAME', 'NAD_geo_tgeo'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION minDistance_transfn(float, tgeometry, tgeometry)
  RETURNS float
  AS 'MODULE_PATHNAME', 'Mindistance_transfn'
  LANGUAGE C IMMUTABLE PARALLEL SAFE;
CREATE AGGREGATE minDistance(tgeometry, tgeometry) (
  SFUNC = minDistance_transfn,
  STYPE = float,
  COMBINEFUNC = float8smaller,
  PARALLEL = SAFE
);

CREATE FUNCTION minDistance_transfn(float, tgeography, tgeography)
  RETURNS float
  AS 'MODULE_PATHNAME', 'Mindistance_transfn'
  LANGUAGE C IMMUTABLE PARALLEL SAFE;
CREATE AGGREGATE minDistance(tgeography, tgeography) (
  SFUNC = minDistance_transfn,
  STYPE = float,
  COMBINEFUNC = float8smaller,
  PARALLEL = SAFE
);

CREATE FUNCTION eDwithinPairs(tgeometry[], tgeometry[], dist float,
    OUT i integer, OUT j integer)
  RETURNS setof record
  AS 'MODULE_PATHNAME', 'Edwithin_tgeoarr_tgeoarr'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION aDwithinPairs(tgeometry[], tgeometry[], dist float,
    OUT i integer, OUT j integer)
  RETURNS setof record
  AS 'MODULE_PATHNAME', 'Adwithin_tgeoarr_tgeoarr'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION eIntersectsPairs(tgeometry[], tgeometry[],
    OUT i integer, OUT j integer)
  RETURNS setof record
  AS 'MODULE_PATHNAME', 'Eintersects_tgeoarr_tgeoarr'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION aIntersectsPairs(tgeometry[], tgeometry[],
    OUT i integer, OUT j integer)
  RETURNS setof record
  AS 'MODULE_PATHNAME', 'Aintersects_tgeoarr_tgeoarr'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION eDisjointPairs(tgeometry[], tgeometry[],
    OUT i integer, OUT j integer)
  RETURNS setof record
  AS 'MODULE_PATHNAME', 'Edisjoint_tgeoarr_tgeoarr'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION aDisjointPairs(tgeometry[], tgeometry[],
    OUT i integer, OUT j integer)
  RETURNS setof record
  AS 'MODULE_PATHNAME', 'Adisjoint_tgeoarr_tgeoarr'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION eDwithinPairs(tgeography[], tgeography[], dist float,
    OUT i integer, OUT j integer)
  RETURNS setof record
  AS 'MODULE_PATHNAME', 'Edwithin_tgeoarr_tgeoarr'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION aDwithinPairs(tgeography[], tgeography[], dist float,
    OUT i integer, OUT j integer)
  RETURNS setof record
  AS 'MODULE_PATHNAME', 'Adwithin_tgeoarr_tgeoarr'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION eIntersectsPairs(tgeography[], tgeography[],
    OUT i integer, OUT j integer)
  RETURNS setof record
  AS 'MODULE_PATHNAME', 'Eintersects_tgeoarr_tgeoarr'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION aIntersectsPairs(tgeography[], tgeography[],
    OUT i integer, OUT j integer)
  RETURNS setof record
  AS 'MODULE_PATHNAME', 'Aintersects_tgeoarr_tgeoarr'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION eDisjointPairs(tgeography[], tgeography[],
    OUT i integer, OUT j integer)
  RETURNS setof record
  AS 'MODULE_PATHNAME', 'Edisjoint_tgeoarr_tgeoarr'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION aDisjointPairs(tgeography[], tgeography[],
    OUT i integer, OUT j integer)
  RETURNS setof record
  AS 'MODULE_PATHNAME', 'Adisjoint_tgeoarr_tgeoarr'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION eTouchesPairs(tgeometry[], tgeometry[],
    OUT i integer, OUT j integer)
  RETURNS setof record
  AS 'MODULE_PATHNAME', 'Etouches_tgeoarr_tgeoarr'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION aTouchesPairs(tgeometry[], tgeometry[],
    OUT i integer, OUT j integer)
  RETURNS setof record
  AS 'MODULE_PATHNAME', 'Atouches_tgeoarr_tgeoarr'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION tDwithinPairs(tgeometry[], tgeometry[], dist float,
    OUT i integer, OUT j integer, OUT periods tstzspanset)
  RETURNS setof record
  AS 'MODULE_PATHNAME', 'Tdwithin_tgeoarr_tgeoarr'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION tIntersectsPairs(tgeometry[], tgeometry[],
    OUT i integer, OUT j integer, OUT periods tstzspanset)
  RETURNS setof record
  AS 'MODULE_PATHNAME', 'Tintersects_tgeoarr_tgeoarr'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION tDisjointPairs(tgeometry[], tgeometry[],
    OUT i integer, OUT j integer, OUT periods tstzspanset)
  RETURNS setof record
  AS 'MODULE_PATHNAME', 'Tdisjoint_tgeoarr_tgeoarr'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION tIntersectsPairs(tgeography[], tgeography[],
    OUT i integer, OUT j integer, OUT periods tstzspanset)
  RETURNS setof record
  AS 'MODULE_PATHNAME', 'Tintersects_tgeoarr_tgeoarr'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION tDisjointPairs(tgeography[], tgeography[],
    OUT i integer, OUT j integer, OUT periods tstzspanset)
  RETURNS setof record
  AS 'MODULE_PATHNAME', 'Tdisjoint_tgeoarr_tgeoarr'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;

CREATE FUNCTION tTouchesPairs(tgeometry[], tgeometry[],
    OUT i integer, OUT j integer, OUT periods tstzspanset)
  RETURNS setof record
  AS 'MODULE_PATHNAME', 'Ttouches_tgeoarr_tgeoarr'
  LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;
