include_directories("liblwgeom")
if(NOT MEOS)
  include_directories("libpgcommon")
endif()
include_directories("ryu")

add_subdirectory("liblwgeom")
if(NOT MEOS)
  add_subdirectory("libpgcommon")
endif()
add_subdirectory("ryu")
